# Market Pulse - Cursor AI 開發規則

## 專案概述
Market Pulse 是一個專為市集攤販設計的離線優先數位助手。
核心原則：3秒操作、100%離線、日系美學。

## 技術棧
- Next.js 14+ (App Router)
- TypeScript (嚴格模式)
- Tailwind CSS
- Dexie.js (IndexedDB)
- Zustand (狀態管理)
- Lucide React (圖標)

## 設計系統規範

### 必須遵循的色彩
- 主色：#7B9FA6 (霧藍)
- 次色：#D4A574 (暖木)
- 背景：#FAFAF8 (米白)
- 輔助色：#F5E6E8 (柔粉)、#E8F3E8 (柔綠)、#FFF8E7 (柔黃)

### 圓角系統
- 主卡片：rounded-[1.5rem] (24px)
- 次卡片：rounded-[1.25rem] (20px)
- 按鈕：rounded-2xl (16px)
- 標籤：rounded-full

### 陰影
- 主卡片：shadow-lg shadow-[#7B9FA6]/10
- 次卡片：shadow-md shadow-[#7B9FA6]/5
- Hover：hover:shadow-xl

## 程式碼規範

### 組件結構
```tsx
'use client'; // 客戶端組件必須標記

import { Icon } from 'lucide-react';

interface ComponentProps {
  // 定義 props
}

export function ComponentName({ props }: ComponentProps) {
  return (
    // JSX
  );
}
```

### 頁面結構
```tsx
export default function PageName() {
  return (
    <div className="min-h-screen bg-[#FAFAF8]">
      {/* Header with gradient */}
      <div className="bg-gradient-to-br from-[#7B9FA6] to-[#D4A574] pt-12 pb-8 px-6 rounded-b-[2rem]">
        <div className="max-w-lg mx-auto">
          <h1 className="text-2xl font-medium text-white opacity-90">標題</h1>
        </div>
      </div>

      {/* Content */}
      <div className="max-w-lg mx-auto px-6 -mt-4">
        {/* 內容 */}
      </div>
    </div>
  );
}
```

### 命名規範
- 組件：PascalCase (例：BottomNavigation)
- 函數：camelCase (例：formatDate)
- 常數：UPPER_SNAKE_CASE (例：MAX_ITEMS)
- 檔案：與組件同名或小寫 (page.tsx, utils.ts)

## 開發約束

### 禁止事項
❌ 不允許圖片上傳或 Base64 儲存
❌ 不使用外部 API（離線優先）
❌ 不使用 Emoji 圖片（使用 Lucide Icons）
❌ 不偏離設計系統色彩

### 必須遵循
✅ 所有資料存於 IndexedDB (Dexie.js)
✅ 使用事件溯源模式
✅ 單手操作優化（按鈕至少 44x44px）
✅ 頁面底部預留 pb-24 給導航
✅ 使用 max-w-lg mx-auto 限制內容寬度
✅ 添加適當的 transition 動畫

## 工具函數使用

### 類名合併
```tsx
import { cn } from '@/lib/utils';
className={cn('base-class', condition && 'conditional-class')}
```

### 格式化
```tsx
import { formatDate, formatCurrency, formatNumber } from '@/lib/utils';
formatDate(new Date())        // "2026/01/21"
formatCurrency(1000)          // "NT$1,000"
formatNumber(1234567)         // "1,234,567"
```

## 移動裝置優化

### 必須檢查
- [ ] 使用 max-w-lg mx-auto
- [ ] 頁面底部 pb-24
- [ ] 按鈕尺寸足夠大
- [ ] 添加 hover 和 transition
- [ ] 使用大圓角
- [ ] 適當的 emoji 使用

## 資料庫規範

### 事件溯源架構
- ✅ 所有操作記錄為不可變事件
- ✅ 維護快照表以供快速讀取
- ✅ 使用 transaction 確保原子性
- ✅ 可從事件歷史重建快照

### 資料表結構
- **events**: 歷史事件（不可變）
- **markets**: 市集快照
- **products**: 商品清單（⚠️ 禁止圖片欄位）
- **dailyStats**: 每日統計
- **settings**: 用戶設定

### 使用規範
```typescript
// ✅ 正確：使用 Hooks
import { useMarkets, createMarket } from '@/lib/db/hooks';
const markets = useMarkets();
await createMarket(data);

// ❌ 錯誤：直接修改資料庫
await db.markets.add(data); // 應該使用 createMarket()

// ✅ 正確：記錄事件
await recordEvent('market_created', payload);

// ✅ 正確：查詢資料
const market = await db.markets.get(id);
```

### 事件類型
- 市集：market_created, market_status_changed, market_started, market_ended
- 商品：product_created, product_updated, product_deleted
- 互動：interaction_recorded, deal_closed
- 設定：settings_updated

## 參考文件優先級

1. PROJECT_CONTEXT.md - 專案核心原則
2. JAPANESE_UI_DESIGN_SYSTEM.md - 設計系統
3. QUICK_START.md - 開發指南
4. STEP1_COMPLETION_REPORT.md - 當前進度

## AI 助手指引

當協助開發時：
1. 始終參考設計系統文件
2. 保持日系美學風格
3. 優先考慮移動裝置體驗
4. 確保離線優先原則
5. 遵循 3 秒操作原則
6. 使用 TypeScript 嚴格型別
7. 添加適當的註解說明

## 提交訊息規範

```
feat: 新增功能
fix: 修復錯誤
style: 樣式調整
refactor: 重構程式碼
docs: 文件更新
test: 測試相關
chore: 雜項任務
```

---

遵循這些規則，確保專案保持一致性與高品質！
