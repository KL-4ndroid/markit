# C2.29B Staff View / RLS Read-Only Verification Result

Date:
Environment:
Runner:
Supabase project:
Owner id:
Staff id:

SQL source:

- `supabase/verification/c2_29b_staff_view_rls_read_only.sql`

Safety confirmation:

- [ ] Only read-only SQL was executed.
- [ ] `BEGIN` / `SET LOCAL` / `ROLLBACK` blocks were used only for auth simulation.
- [ ] No migration was executed.
- [ ] No RLS/policy/function/schema change was executed.
- [ ] No `INSERT`, `UPDATE`, `DELETE`, `UPSERT`, or data repair was executed.

## C2.29B-E0 Candidate Active Owner/Staff Pairs

Result summary:

```text
Row count:
Chosen owner_id:
Chosen staff_id:
Chosen related_market_count:
Chosen staff role:
```

Assessment:

- [ ] Pass: usable active owner/staff pair exists.
- [ ] Blocked: no active staff relationship available.
- [ ] Blocked/error:

Notes:

```text

```

## C2.29B-E1 Staff Market View Redaction And Scope

Result summary:

```text
access_type counts:
total_markets:
booth_cost_null:
total_profit_null:
commission_rate_null:
registration_fee_null:
deposit_null:
duplicate_market_id_rows:
deleted_market_rows:
```

Assessment:

- [ ] Pass: staff sees only staff rows, sensitive fields are null, and no duplicate/deleted rows appear.
- [ ] Pass with notes: zero staff-visible market rows are expected for this account.
- [ ] Needs review: one or more sensitive fields are exposed, owner rows appear, duplicates appear, or deleted rows appear.
- [ ] Blocked/error:

Notes:

```text

```

## C2.29B-E2 Staff Product View Redaction

Result summary:

```text
staff_products_view:
total_products:
cost_null:
```

Assessment:

- [ ] Pass: if products exist, `cost_null = total_products`.
- [ ] Pass with notes: zero staff-visible products are expected for this account.
- [ ] Needs review: product cost is exposed.
- [ ] Blocked/error:

Notes:

```text

```

## C2.29B-E3 Staff Event Payload Redaction, Tombstones, And Duplicate Ids

Result summary:

```text
access_type counts:
total_events:
has_boothCost:
has_booth_cost:
has_registrationFee:
has_registration_fee:
has_deposit:
has_cost:
has_costAtTimeOfSale:
has_cost_at_time_of_sale:
has_supplierInfo:
has_supplier_info:
has_totalCost:
has_total_cost:
has_profitMargin:
has_profit_margin:
has_grossMargin:
has_gross_margin:
has_totalProfit:
has_total_profit:
has_netProfit:
has_net_profit:
has_commissionRate:
has_commission_rate:
has_tableRental:
has_table_rental:
has_chairRental:
has_chair_rental:
has_umbrellaRental:
has_umbrella_rental:
has_tableclothRental:
has_tablecloth_rental:
duplicate_event_id_rows:
tombstone type counts:
```

Assessment:

- [ ] Pass: staff sees only staff rows, all sensitive payload key counts are zero, and duplicate event ids are zero.
- [ ] Pass with notes: zero staff-visible events are expected for this account.
- [ ] Needs review: sensitive payload keys, owner rows, or duplicate event ids appear.
- [ ] Blocked/error:

Notes:

```text

```

## C2.29B-E4 Staff Direct Base-Table SELECT Blocked

Result summary:

```text
staff_markets_direct:
staff_products_direct:
staff_events_direct:
staff_markets_with_booth_cost:
staff_products_with_cost:
staff_events_with_payload:
Errors, if any:
```

Assessment:

- [ ] Pass: all staff direct base-table counts are zero.
- [ ] Needs review: staff can read direct base-table rows.
- [ ] Blocked/error:

Notes:

```text

```

## C2.29B-E4 Owner Direct Base-Table Regression Check

Result summary:

```text
owner_markets_direct:
owner_products_direct:
owner_events_direct:
owner sample rows returned:
financial fields visible to owner:
Errors, if any:
```

Assessment:

- [ ] Pass: owner can still read expected owner-owned base-table rows and financial fields.
- [ ] Pass with notes: chosen owner has limited/no data but no permission regression is shown.
- [ ] Needs review: owner reads are blocked or owner financial fields are unexpectedly hidden.
- [ ] Blocked/error:

Notes:

```text

```

## C2.29B-E5 Type-Level Guard

Command:

```powershell
npx.cmd tsc --noEmit --project tsconfig.staff-typed-client.json
```

Result summary:

```text
Exit code:
Output summary:
```

Assessment:

- [ ] Pass: type-level guard compiles with expected forbidden-base-table assertions.
- [ ] Needs review: type-level guard is missing, stale, or failing.
- [ ] Blocked/error:

Notes:

```text

```

## Overall C2.29B Decision

Overall result:

- [ ] Pass: staff view/RLS verification does not block the next plan.
- [ ] Pass with notes: findings exist but are understood and not blocking.
- [ ] Needs follow-up analysis before continuing.
- [ ] Requires explicit RLS/view/client decision before continuing.

Recommended next step:

```text

```
