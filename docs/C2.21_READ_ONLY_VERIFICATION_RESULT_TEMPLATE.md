# C2.21 Read-Only Cloud Consistency Verification Result

Date:
Environment:
Runner:
Supabase project:

SQL source:

- `supabase/verification/c2_21_cloud_consistency_read_only.sql`

Safety confirmation:

- [ ] Only `SELECT` statements were executed.
- [ ] No migration was executed.
- [ ] No RLS/policy/function/schema change was executed.
- [ ] No `INSERT`, `UPDATE`, `DELETE`, `UPSERT`, or data repair was executed.

## C2.21-A Markets Projection vs Active Deal Events

Result summary:

```text
Row count:
Largest revenue_diff:
Largest deal_diff:
Notable market_ids:
```

Assessment:

- [ ] Pass: zero mismatch rows.
- [ ] Needs review: mismatch rows exist.
- [ ] Blocked/error:

Notes:

```text

```
## C2.21-B Tombstone Completeness

Result summary:

```text
deal_deleted tombstone_count:
deal_deleted missing_root_market_id:
deal_deleted missing_target_event_id:
interaction_deleted tombstone_count:
interaction_deleted missing_root_market_id:
interaction_deleted missing_target_event_id:
```

Assessment:

- [ ] Pass: all missing counts are zero.
- [ ] Needs review: missing root market id or target event id exists.
- [ ] Blocked/error:

Notes:

```text

```

## C2.21-C Tombstone Target Integrity

Result summary:

```text
Row count:
Notable tombstone_ids:
Notable target_event_ids:
```

Assessment:

- [ ] Pass: zero missing target rows.
- [ ] Needs review: tombstones reference missing targets.
- [ ] Blocked/error:

Notes:

```text

```

## C2.21-D Duplicate Semantic Deal Check

Result summary:

```text
Row count:
Highest duplicate_count:
Notable market_ids:
```

Assessment:

- [ ] Pass: zero duplicate semantic deal rows.
- [ ] Needs manual classification: duplicate-like rows exist.
- [ ] Blocked/error:

Notes:

```text

```

## C2.21-E Snapshot Shape Check

Result summary:

```text
Row count:
Observed data_type values:
Observed top_level_key values:
Latest snapshot_at:
```

Assessment:

- [ ] Pass: snapshot shape is understood and compatible with current expectations.
- [ ] Needs review: unexpected shape or unreadable data exists.
- [ ] Blocked/error:

Notes:

```text

```

## Overall C2.21 Decision

Overall result:

- [ ] Pass: no data-debt finding blocks future planning.
- [ ] Pass with notes: findings exist but are understood and not blocking.
- [ ] Needs follow-up analysis before C2.20.
- [ ] Requires explicit repair decision before future replace-cache/cloud-first work.

Recommended next step:

```text

```
