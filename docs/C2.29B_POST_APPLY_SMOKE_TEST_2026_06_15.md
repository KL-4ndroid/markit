# C2.29B-1 Post-Apply Smoke Test

> **狀態**：🟡 框架已建立（2026-06-15），**測試結果待用戶填寫**
> **對應檔案**：`supabase/migrations/039_staff_view_hardening.sql`（套用 commit `439f97f`）
> **對應收尾紀錄**：`docs/C2.29_REANALYSIS_2026_06_15.md` §C2.29B-1 Apply Result
> **對應套用 checklist**：`docs/C2.29B-1_APPLICATION_CHECKLIST.md`
> **用途**：039 套用後的真實 UI 與 Supabase view 行為驗證，確認沒有 regression
> **Cursor 角色**：只**寫框架**（含完整測試記錄表 + Supabase view 驗證 SQL 模板），**不主動登入任何服務、不主動跑 UI smoke test**。實際登入 / 點按鈕 / 截圖由用戶執行。

---

## ⚠️ 帳號安全提醒

測試前與測試後：

| 動作 | 說明 |
|---|---|
| **測試前** | 用戶已提供 Owner / Staff 帳號明文密碼（`123@123` / `1234@1234`）。此密碼強度極弱（3 位純數字），**測試完成後強烈建議立即改密碼**（Owner 跟 Staff 都改） |
| **測試中** | 若在公共裝置執行，登出時勾「保持登入」= **不**打勾 |
| **測試後** | 確認 Supabase session 已登出 / 瀏覽器已關閉 |

---

## 0. 測試基本資料

### 0.1 測試帳號

| 角色 | Email | 密碼 | User UUID（已驗證） | 備註 |
|---|---|---|---|---|
| **Owner** | `123@123` | `123456` | （用戶自填） | 預期在 `staff_relationships.owner_id` 中是 owner |
| **Staff** | `1234@1234` | `123456` | `5e92b457-1eaf-49eb-9295-ba47b5a3e575` | E2 / E3 測試用過的員工 |

> 兩者皆為 E1 線上驗證時使用過的測試帳號（見 `C2.29_REANALYSIS_2026_06_15.md` §9.6.1）。

### 0.2 測試時間

- **開始時間**：（用戶自填）
- **結束時間**：（用戶自填）
- **測試者**：（用戶自填）
- **測試環境**：（用戶自填，例如：Chrome 桌面 / Safari iOS / 等）

### 0.3 測試環境前置確認

| 項目 | 預期 | 確認 |
|---|---|---|
| 039 已套用到 Supabase | `439f97f` 已在 Supabase 生效 | ☐ |
| 用戶端網路正常 | 連得到 Supabase | ☐ |
| 測試 Owner / Staff 帳號可登入 | auth login 成功 | ☐ |
| 測試前先重整瀏覽器 / 清快取 | 避免 stale code 干擾 | ☐ |

---

## 1. Staff UI Smoke Test

> 工具：員工 `1234@1234` 登入 app（用戶執行）

### 1.1 市集列表頁

| # | 測試項目 | 預期 | 實際 | 截圖 | 通過 |
|---|---|---|---|---|---|
| S1.1 | 員工登入後首頁「我的市集」是否載入 | 正常顯示員工所屬市集 | （用戶填） | （檔名） | ☐ |
| S1.2 | 每張市集卡是否顯示市集名稱 / 日期 / 地點 | 正常 | （用戶填） | — | ☐ |
| S1.3 | 每張市集卡**不**顯示 financial 數字（booth_cost / total_profit / commission_rate）| 不顯示 | （用戶填） | — | ☐ |
| S1.4 | 每張市集卡**不**顯示 rental 數字 | 不顯示 | （用戶填） | — | ☐ |
| S1.5 | 點選市集卡是否進入市集詳情 | 正常跳轉 | （用戶填） | — | ☐ |

### 1.2 市集詳情頁（Staff 視角）

| # | 測試項目 | 預期 | 實際 | 截圖 | 通過 |
|---|---|---|---|---|---|
| S2.1 | 市集詳情頁可載入 | 正常 | （用戶填） | — | ☐ |
| S2.2 | 「攤位費 / 報名費 / 租金 / 利潤 / 成本」相關欄位**不**顯示 | 全部不顯示 | （用戶填） | — | ☐ |
| S2.3 | 「保證金 deposit」**顯示**（符合產品決策：員工需看保證金提醒）| 顯示 | （用戶填） | — | ☐ |
| S2.4 | 統計區塊可見（總營收 total_revenue / 互動數 / 成交數）| 顯示 | （用戶填） | — | ☐ |
| S2.5 | 統計區塊**不**顯示 total_profit | 不顯示 | （用戶填） | — | ☐ |
| S2.6 | 員工**沒有**刪除 / 編輯市集入口 | 無 | （用戶填） | — | ☐ |

### 1.3 商品列表頁

| # | 測試項目 | 預期 | 實際 | 截圖 | 通過 |
|---|---|---|---|---|---|
| S3.1 | 商品列表可載入 | 正常 | （用戶填） | — | ☐ |
| S3.2 | 商品卡顯示 name / price / stock | 正常顯示 | （用戶填） | — | ☐ |
| S3.3 | 商品卡**不**顯示 cost | 不顯示 | （用戶填） | — | ☐ |
| S3.4 | 點選商品卡可看到詳情 | 正常跳轉 | （用戶填） | — | ☐ |
| S3.5 | 商品詳情**不**顯示 cost | 不顯示 | （用戶填） | — | ☐ |

### 1.4 收入 / 利潤 / 成本敏感資料

| # | 測試項目 | 預期 | 實際 | 截圖 | 通過 |
|---|---|---|---|---|---|
| S4.1 | 員工首頁 / dashboard 任何地方搜尋「成本」| 不顯示 | （用戶填） | — | ☐ |
| S4.2 | 員工首頁 / dashboard 任何地方搜尋「利潤」| 不顯示 | （用戶填） | — | ☐ |
| S4.3 | 員工首頁 / dashboard 任何地方搜尋「毛利率」| 不顯示 | （用戶填） | — | ☐ |
| S4.4 | 員工首頁 / dashboard 任何地方搜尋「攤位費」| 不顯示 | （用戶填） | — | ☐ |
| S4.5 | 員工首頁 / dashboard 任何地方搜尋「租金」| 不顯示 | （用戶填） | — | ☐ |

### 1.5 重新整理 / 重新登入

| # | 測試項目 | 預期 | 實際 | 截圖 | 通過 |
|---|---|---|---|---|---|
| S5.1 | 在市集詳情頁按重新整理（F5）| 正常載入，敏感欄位仍不顯示 | （用戶填） | — | ☐ |
| S5.2 | 強制 reload（Ctrl+Shift+R / Cmd+Shift+R）| 正常載入，敏感欄位仍不顯示 | （用戶填） | — | ☐ |
| S5.3 | 登出 → 重新登入 → 進入市集詳情 | 正常載入，敏感欄位仍不顯示 | （用戶填） | — | ☐ |
| S5.4 | 關閉瀏覽器 → 重新打開 → 進入市集詳情 | 正常載入，敏感欄位仍不顯示 | （用戶填） | — | ☐ |

### 1.6 Staff UI 結果摘要

| 項目 | 結果 |
|---|---|
| 總測試項 | 18 |
| 通過 | （用戶填） |
| 失敗 | （用戶填） |
| UI regression 發現 | （用戶填：☐ 無 / ☐ 有，詳情見 §5）|

---

## 2. Owner UI Smoke Test

> 工具：Owner `123@123` 登入 app（用戶執行）

### 2.1 市集列表頁

| # | 測試項目 | 預期 | 實際 | 截圖 | 通過 |
|---|---|---|---|---|---|
| O1.1 | Owner 登入後首頁「我的市集」是否載入 | 正常顯示 | （用戶填） | — | ☐ |
| O1.2 | 每張市集卡顯示市集名稱 / 日期 / 地點 | 正常 | （用戶填） | — | ☐ |
| O1.3 | 每張市集卡**顯示** financial 數字（booth_cost / total_profit / commission_rate）| **顯示真實值** | （用戶填） | — | ☐ |
| O1.4 | 每張市集卡**顯示** rental 數字 | 顯示 | （用戶填） | — | ☐ |
| O1.5 | 點選市集卡進入市集詳情 | 正常跳轉 | （用戶填） | — | ☐ |

### 2.2 市集詳情頁（Owner 視角）

| # | 測試項目 | 預期 | 實際 | 截圖 | 通過 |
|---|---|---|---|---|---|
| O2.1 | 市集詳情頁可載入 | 正常 | （用戶填） | — | ☐ |
| O2.2 | 「攤位費 / 報名費 / 租金 / 利潤 / 成本」**全部顯示** | 顯示 | （用戶填） | — | ☐ |
| O2.3 | 「保證金 deposit」顯示 | 顯示 | （用戶填） | — | ☐ |
| O2.4 | 統計區塊（total_revenue / total_profit / total_deals / total_interactions）全部顯示 | 顯示 | （用戶填） | — | ☐ |
| O2.5 | Owner 有刪除 / 編輯市集入口 | 有 | （用戶填） | — | ☐ |
| O2.6 | Owner 有變更市集狀態的入口 | 有 | （用戶填） | — | ☐ |

### 2.3 商品建立 / 編輯

| # | 測試項目 | 預期 | 實際 | 截圖 | 通過 |
|---|---|---|---|---|---|
| O3.1 | Owner 可建立新商品（填 name / price / cost）| 正常建立 | （用戶填） | — | ☐ |
| O3.2 | 建立後商品 cost 在 Owner 視角下可見 | 可見 | （用戶填） | — | ☐ |
| O3.3 | Owner 可編輯既有商品（改 name / price / cost）| 正常編輯 | （用戶填） | — | ☐ |
| O3.4 | 編輯後 cost 更新 | 正確 | （用戶填） | — | ☐ |

### 2.4 成交 / 互動

| # | 測試項目 | 預期 | 實際 | 截圖 | 通過 |
|---|---|---|---|---|---|
| O4.1 | Owner 可關閉一筆交易 | 正常 | （用戶填） | — | ☐ |
| O4.2 | 成交後 DailyTransactionLog 出現新紀錄 | 出現 | （用戶填） | — | ☐ |
| O4.3 | Owner 可看到既有成交紀錄完整資訊（含 items / cost / profit） | 完整 | （用戶填） | — | ☐ |
| O4.4 | Owner 可記錄互動 | 正常 | （用戶填） | — | ☐ |

### 2.5 analytics / revenue / profit

| # | 測試項目 | 預期 | 實際 | 截圖 | 通過 |
|---|---|---|---|---|---|
| O5.1 | Analytics 頁面可載入 | 正常 | （用戶填） | — | ☐ |
| O5.2 | 收入 / 利潤 / 毛利率 / 淨利圖表顯示 | 顯示 | （用戶填） | — | ☐ |
| O5.3 | 圖表數據正確（與實際交易吻合）| 正確 | （用戶填） | — | ☐ |

### 2.6 Owner UI 結果摘要

| 項目 | 結果 |
|---|---|
| 總測試項 | 17 |
| 通過 | （用戶填） |
| 失敗 | （用戶填） |
| UI regression 發現 | （用戶填：☐ 無 / ☐ 有，詳情見 §5）|

---

## 3. Supabase View 驗證重跑

> 工具：Supabase SQL Editor（用戶執行；可選 Cursor 透過 MCP 跑 — **但需用戶授權**）
> 對應 commit：`439f97f`

### 3.1 套用狀態確認

```sql
-- 確認 039 套用版本
SELECT viewname, length(definition) AS def_length
FROM pg_views
WHERE schemaname = 'public'
  AND viewname IN (
    'staff_accessible_markets',
    'staff_accessible_products',
    'staff_accessible_events'
  )
ORDER BY viewname;
```

**預期**：

| View | 預期 def_length | 實際 | 對齊備份文件？ |
|---|---|---|---|
| `staff_accessible_markets` | 與套用後版本一致（**大於**備份文件 §1.1 的 baseline）| （用戶填） | ☐ |
| `staff_accessible_products` | 與套用後版本一致（**大於**備份文件 §2.1）| （用戶填） | ☐ |
| `staff_accessible_events` | 與套用後版本一致（**大於**備份文件 §3.1）| （用戶填） | ☐ |

**對照基線**：`docs/C2.29_VIEW_BACKUP_2026_06_15.md` §1.1 / §2.1 / §3.1 的 def_length。

### 3.2 Staff 驗證（5 個 block，每個 block 獨立 transaction）

#### 3.2.1 驗證 1：Staff markets 敏感欄位為 NULL

```sql
BEGIN;
SET LOCAL ROLE authenticated;
SELECT set_config('request.jwt.claim.sub', '5e92b457-1eaf-49eb-9295-ba47b5a3e575', true);

SELECT
    name,
    booth_cost,            -- 預期：NULL
    commission_rate,       -- 預期：NULL
    total_profit,          -- 預期：NULL
    table_rental,          -- 預期：NULL
    chair_rental,          -- 預期：NULL
    umbrella_rental,       -- 預期：NULL
    tablecloth_rental,     -- 預期：NULL
    registration_fee,      -- 預期：NULL
    deposit,               -- 預期：保留
    total_revenue,         -- 預期：保留
    total_deals,           -- 預期：保留
    total_interactions     -- 預期：保留
FROM staff_accessible_markets
LIMIT 1;
ROLLBACK;
```

**結果**：

| 欄位 | 預期 | 實際 | 通過 |
|---|---|---|---|
| booth_cost | NULL | （用戶填） | ☐ |
| commission_rate | NULL | （用戶填） | ☐ |
| total_profit | NULL | （用戶填） | ☐ |
| table_rental | NULL | （用戶填） | ☐ |
| chair_rental | NULL | （用戶填） | ☐ |
| umbrella_rental | NULL | （用戶填） | ☐ |
| tablecloth_rental | NULL | （用戶填） | ☐ |
| registration_fee | NULL | （用戶填） | ☐ |
| deposit | 真實值 | （用戶填） | ☐ |
| total_revenue | 真實值 | （用戶填） | ☐ |
| total_deals | 真實值 | （用戶填） | ☐ |
| total_interactions | 真實值 | （用戶填） | ☐ |

#### 3.2.2 驗證 2：Staff products.cost 為 NULL

```sql
BEGIN;
SET LOCAL ROLE authenticated;
SELECT set_config('request.jwt.claim.sub', '5e92b457-1eaf-49eb-9295-ba47b5a3e575', true);

SELECT name, price, cost, stock, total_sold
FROM staff_accessible_products
LIMIT 1;
ROLLBACK;
```

**結果**：

| 欄位 | 預期 | 實際 | 通過 |
|---|---|---|---|
| cost | NULL | （用戶填） | ☐ |
| price | 真實值 | （用戶填） | ☐ |
| stock | 真實值 | （用戶填） | ☐ |
| total_sold | 真實值 | （用戶填） | ☐ |

#### 3.2.3 驗證 3：Staff events.payload 敏感 key 移除

```sql
BEGIN;
SET LOCAL ROLE authenticated;
SELECT set_config('request.jwt.claim.sub', '5e92b457-1eaf-49eb-9295-ba47b5a3e575', true);

-- 3a. top-level 敏感 key
SELECT
    type,
    payload ? 'boothCost'            AS has_boothCost,
    payload ? 'cost'                 AS has_cost,
    payload ? 'costAtTimeOfSale'     AS has_costAtTimeOfSale,
    payload ? 'supplierInfo'         AS has_supplierInfo,
    payload ? 'profitMargin'         AS has_profitMargin,
    payload ? 'grossMargin'          AS has_grossMargin,
    payload ? 'totalProfit'          AS has_totalProfit,
    payload ? 'booth_cost'           AS has_booth_cost,
    payload ? 'commissionRate'       AS has_commissionRate
FROM staff_accessible_events
WHERE type = 'market_created'
LIMIT 5;
ROLLBACK;
```

**結果**：

| Key | 預期 has_* | 實際 | 通過 |
|---|---|---|---|
| `boothCost` | false | （用戶填） | ☐ |
| `cost` | false | （用戶填） | ☐ |
| `costAtTimeOfSale` | false | （用戶填） | ☐ |
| `supplierInfo` | false | （用戶填） | ☐ |
| `profitMargin` | false | （用戶填） | ☐ |
| `grossMargin` | false | （用戶填） | ☐ |
| `totalProfit` | false | （用戶填） | ☐ |
| `booth_cost` | false | （用戶填） | ☐ |
| `commissionRate` | false | （用戶填） | ☐ |

#### 3.2.4 驗證 3b：Staff events.payload nested items[] 敏感 key 移除

```sql
BEGIN;
SET LOCAL ROLE authenticated;
SELECT set_config('request.jwt.claim.sub', '5e92b457-1eaf-49eb-9295-ba47b5a3e575', true);

SELECT
    type,
    jsonb_path_query_array(payload, '$.items[*]') AS items_array
FROM staff_accessible_events
WHERE type = 'deal_closed'
LIMIT 5;
ROLLBACK;
```

**結果**：

- items_array 中是否含 `cost` / `costAtTimeOfSale` / `supplierInfo` / `profitMargin` 等敏感 key？
  - 預期：**不含**
  - 實際：（用戶填）
  - 通過：☐

#### 3.2.5 驗證 4：Staff 仍看到 tombstone

```sql
BEGIN;
SET LOCAL ROLE authenticated;
SELECT set_config('request.jwt.claim.sub', '5e92b457-1eaf-49eb-9295-ba47b5a3e575', true);

SELECT type, count(*)
FROM staff_accessible_events
WHERE type IN ('deal_deleted', 'interaction_deleted')
GROUP BY type;
ROLLBACK;
```

**結果**：

- 預期：至少有 1 個 `deal_deleted` 或 `interaction_deleted` row（沒資料也算通過）
- 實際：（用戶填）
- 通過：☐

#### 3.2.6 驗證 5：Owner 仍可看到完整資料

```sql
BEGIN;
SET LOCAL ROLE authenticated;
SELECT set_config('request.jwt.claim.sub', 'OWNER_UUID', true);  -- 換成實際 owner UUID

-- 5a. Owner markets
SELECT name, booth_cost, total_profit, commission_rate
FROM staff_accessible_markets
LIMIT 1;

-- 5b. Owner products
SELECT name, cost, price
FROM staff_accessible_products
LIMIT 1;

-- 5c. Owner events payload
SELECT type, payload
FROM staff_accessible_events
LIMIT 1;
ROLLBACK;
```

**結果**：

| 項目 | 預期 | 實際 | 通過 |
|---|---|---|---|
| Owner markets.booth_cost | 真實值 | （用戶填） | ☐ |
| Owner markets.total_profit | 真實值 | （用戶填） | ☐ |
| Owner markets.commission_rate | 真實值 | （用戶填） | ☐ |
| Owner products.cost | 真實值 | （用戶填） | ☐ |
| Owner events.payload | 完整 JSON | （用戶填） | ☐ |

### 3.3 Supabase view 驗證結果摘要

| 驗證 block | 預期 | 實際 | 通過 |
|---|---|---|---|
| 3.1 套用狀態 | 3 view def 與套用版本一致 | （用戶填） | ☐ |
| 3.2.1 Staff markets 8 個 NULL + 4 個保留 | 8 + 4 | （用戶填） | ☐ |
| 3.2.2 Staff products.cost NULL | NULL | （用戶填） | ☐ |
| 3.2.3 Staff events payload 9 個 key 移除 | 全部 false | （用戶填） | ☐ |
| 3.2.4 Staff events nested items[] 脫敏 | 不含敏感 key | （用戶填） | ☐ |
| 3.2.5 Staff tombstone 仍可見 | 至少 0 row | （用戶填） | ☐ |
| 3.2.6 Owner 完整資料 | 全部真實值 | （用戶填） | ☐ |

---

## 4. 已知風險保留

> 必須在最終總結時複製這段警告

```
C2.29B-1 只修 view 層。
E2 已證明 Staff 仍可透過底表 RLS 直接 SELECT markets 取得敏感欄位。
底表 RLS / direct table access 仍是 C2.29B-2 範圍。
```

**重點**：

- ✅ C2.29B-1 已消除 3 個攻擊面（view 層 #1-#3：staff_accessible_markets / products / events 敏感欄位洩漏）
- ⚠️ **攻擊面 #4 仍存在**：員工 DevTools 仍可繞過 view 透過底表 RLS 直接 SELECT `markets` / `products` / `events` 取得敏感欄位
- ⏸️ C2.29B-2 範圍（**未實作**）：
  1. 底表 RLS 收緊（草稿 B 見 `C2.29_REANALYSIS_2026_06_15.md` §7.2）
  2. 前端查詢路徑收斂（改走 `staff_accessible_*` view 或 owner-only view）
  3. 或全面改走 SECURITY DEFINER RPC

---

## 5. Regression 紀錄

> 若 §1 / §2 / §3 任何項目失敗，請填這裡

### 5.1 UI Regression（Staff / Owner）

| # | Regression 描述 | 嚴重度 | 對應測試項 | 暫時緩解 | 永久修復 |
|---|---|---|---|---|---|
| 1 | （用戶填） | （用戶填） | （用戶填） | （用戶填） | （用戶填） |
| 2 | | | | | |

### 5.2 Supabase view Regression

| # | Regression 描述 | 嚴重度 | 對應驗證 block | 暫時緩解 | 永久修復 |
|---|---|---|---|---|---|
| 1 | （用戶填） | （用戶填） | （用戶填） | （用戶填） | （用戶填） |
| 2 | | | | | |

### 5.3 暫時緩解 vs 永久修復

- 暫時緩解：用 ROLLBACK 還原 039 套用（見 `C2.29_VIEW_BACKUP_2026_06_15.md`）
- 永久修復：修改 039 草稿 → commit → 用戶再套用 → 重跑本文件所有測試

---

## 6. C2.29B-2 待辦摘要（不實作）

> **本階段不做 C2.29B-2**，僅彙整待辦事項

| # | 待辦 | 範圍 | 優先度 | 預估 commit |
|---|---|---|---|---|
| 1 | 底表 RLS 收緊 | `markets` / `products` / `events` RLS policy | 🟠 P0 | 新 migration（可能 040 或 041） |
| 2 | 前端查詢路徑收斂 | `lib/supabase/markets.ts` / `products.ts` / events 相關查詢 | 🟡 P1 | 多次 commit |
| 3 | 全面改走 SECURITY DEFINER RPC | 所有讀取查詢 | 🟡 P1（長期）| 多次 commit |
| 4 | 員工視角下底表攻擊面 #4 驗證 | E2 線上實測已做；C2.29B-2 後再驗一次 | 🟠 P0 | 文件 |

**C2.29B-2 啟動條件**（見 `C2.29_REANALYSIS_2026_06_15.md` §9.8.3）：

- [x] 039 已套用到 Supabase（2026-06-15 ✅）
- [x] 039 套用後驗證 SQL 全部通過（2026-06-15 ✅）
- [ ] 本 smoke test 全部通過（**待用戶回報**）
- [ ] 前端審計完成（§7 步驟 1-4）
- [ ] 用戶明確授權 C2.29B-2 啟動

---

## 7. 測試結果總結

### 7.1 整體通過率

| 區塊 | 總項 | 通過 | 失敗 | 通過率 |
|---|---|---|---|---|
| 1. Staff UI smoke test | 18 | （用戶填） | （用戶填） | （用戶填） |
| 2. Owner UI smoke test | 17 | （用戶填） | （用戶填） | （用戶填） |
| 3. Supabase view 驗證 | 7 | （用戶填） | （用戶填） | （用戶填） |
| **總計** | **42** | （用戶填） | （用戶填） | （用戶填） |

### 7.2 最終判定

- ☐ **全部通過** → C2.29B-1 收尾完成，可進入 C2.29B-2 規劃或 C3 phase
- ☐ **部分失敗**（僅 Supabase view 驗證失敗） → 039 套用可能有問題，評估 ROLLBACK
- ☐ **部分失敗**（僅 UI 失敗） → 可能是前端 stale code / 瀏覽器快取問題，清快取重試
- ☐ **多項失敗** → 暫停 C2.29B-1 收尾，評估是否需 ROLLBACK

### 7.3 測試者簽名

| 角色 | 姓名 | 日期 | 簽名 / 確認 |
|---|---|---|---|
| 測試執行 | （用戶） | （用戶填） | ☐ |
| 結果彙整 | Cursor | 2026-06-15 | ✅ 框架建立完成 |

---

## 8. Cursor 狀態

本文件建立後：

- ✅ Cursor 只**寫框架**，**不主動登入任何服務**
- ✅ Cursor **不**呼叫 `cursor-ide-browser` 跳出瀏覽器
- ✅ Cursor **不**主動跑 Supabase 驗證 SQL（除非用戶明確授權）
- ⏸️ 等用戶跑完 §1-§3 測試後回報結果，再請用戶填寫本文

---

## 9. 相關文件

- `supabase/migrations/039_staff_view_hardening.sql` — 套用對象
- `docs/C2.29_REANALYSIS_2026_06_15.md` — C2.29 完整分析 + C2.29B-1 套用紀錄
- `docs/C2.29_VIEW_BACKUP_2026_06_15.md` — 線上 view 定義備份 + rollback 依據
- `docs/C2.29B-1_APPLICATION_CHECKLIST.md` — 套用 step-by-step checklist
- `docs/OWNER_STAFF_REVENUE_HARDENING_PLAN.md` — C2.23-C2.29 主計畫
- `docs/CONVERGENCE_ARCHIVE.md` — 收斂文件總檔
