# C2.20 Staff Data Flow Read-Only Verification Result

Date:
Environment:
Runner:
Supabase project:
Owner id:
Staff id:

SQL source:

- `supabase/verification/c2_20_staff_data_flow_read_only.sql`

Safety confirmation:

- [ ] Only read-only SQL was executed.
- [ ] `BEGIN` / `SET LOCAL` / `ROLLBACK` blocks were used only for auth simulation.
- [ ] No migration was executed.
- [ ] No RLS/policy/function/schema change was executed.
- [ ] No `INSERT`, `UPDATE`, `DELETE`, `UPSERT`, or data repair was executed.

## C2.20-0 Candidate Active Owner/Staff Pairs

Result summary:

```text
Row count:
Chosen owner_id:
Chosen staff_id:
Chosen related_market_count:
Chosen staff role:
```

Assessment:

- [ ] Pass: usable active owner/staff pair exists.
- [ ] Blocked: no active staff relationship available.
- [ ] Blocked/error:

Notes:

```text

```
## C2.20-A Staff View Definitions

Result summary:

```text
staff_accessible_markets definition returned: yes/no
staff_accessible_products definition returned: yes/no
staff_accessible_events definition returned: yes/no
sanitize_staff_event_payload function returned: yes/no
```

Assessment:

- [ ] Pass: view/function definitions are present and consistent with staff data flow expectations.
- [ ] Needs review: definition exists but has unexpected scope/sanitizer behavior.
- [ ] Blocked/error:

Notes:

```text

```

## C2.20-B Tombstone Root Market Id Coverage

Result summary:

```text
deal_deleted tombstone_count:
deal_deleted missing_root_market_id:
deal_deleted missing_target_event_id:
interaction_deleted tombstone_count:
interaction_deleted missing_root_market_id:
interaction_deleted missing_target_event_id:
```

Assessment:

- [ ] Pass: all missing counts are zero.
- [ ] Needs review: missing root market id or target event id exists.
- [ ] Blocked/error:

Notes:

```text

```

## C2.20-C Tombstones Missing Root Market Id

Result summary:

```text
Row count:
Notable event ids:
```

Assessment:

- [ ] Pass: zero rows.
- [ ] Needs review: missing-root tombstones exist.
- [ ] Blocked/error:

Notes:

```text

```

## C2.20-D Explicit Owner/Staff Relationship Event Coverage

Result summary:

```text
deal_closed event_count:
deal_deleted event_count:
interaction_recorded event_count:
interaction_deleted event_count:
missing_root_market_id values:
```

Assessment:

- [ ] Pass: relationship-scoped event coverage is present and internally consistent.
- [ ] Pass with notes: selected staff has limited/no tombstone coverage but no contradiction was found.
- [ ] Needs review: relationship coverage contradicts staff view output.
- [ ] Blocked/error:

Notes:

```text

```

## C2.20-E Staff-Session View Counts

Result summary:

```text
staff_markets_view:
staff_products_view:
staff_events_view:
event type counts:
```

Assessment:

- [ ] Pass: staff views return expected scoped rows.
- [ ] Pass with notes: zero rows are expected for chosen staff.
- [ ] Needs review: counts are unexpectedly zero or unexpectedly broad.
- [ ] Blocked/error:

Notes:

```text

```

## C2.20-F Staff-Session Tombstone Visibility

Result summary:

```text
deal_deleted tombstone_count:
deal_deleted missing_root_market_id:
deal_deleted missing_target_event_id:
interaction_deleted tombstone_count:
interaction_deleted missing_root_market_id:
interaction_deleted missing_target_event_id:
Sample rows returned:
```

Assessment:

- [ ] Pass: staff-visible tombstones are present when expected and preserve target ids.
- [ ] Pass with notes: no tombstones exist for chosen staff scope.
- [ ] Needs review: explicit relationship query shows tombstones but staff view does not.
- [ ] Blocked/error:

Notes:

```text

```

## C2.20-G Staff-Session Event Payload Sensitive-Key Check

Result summary:

```text
total_events:
has_boothCost:
has_cost:
has_costAtTimeOfSale:
has_supplierInfo:
has_profitMargin:
has_grossMargin:
has_totalProfit:
has_netProfit:
has_deposit:
has_commissionRate:
has_tableRental:
has_chairRental:
has_umbrellaRental:
has_tableclothRental:
has_registrationFee:
has_booth_cost:
has_total_cost:
has_supplier_info:
```

Assessment:

- [ ] Pass: all sensitive key counts are zero.
- [ ] Needs review: one or more sensitive keys appear in staff-visible payloads.
- [ ] Blocked/error:

Notes:

```text

```

## C2.20-H Staff-Session Market/Product Sensitive-Column Check

Result summary:

```text
total_markets:
booth_cost_null:
total_profit_null:
commission_rate_null:
registration_fee_null:
total_products:
cost_null:
```

Assessment:

- [ ] Pass: sensitive columns are null for staff-visible market/product rows.
- [ ] Needs review: staff-visible rows expose sensitive columns.
- [ ] Blocked/error:

Notes:

```text

```

## Overall C2.20 Decision

Overall result:

- [ ] Pass: staff data flow verification does not block C2.29B.
- [ ] Pass with notes: findings exist but are understood and not blocking C2.29B.
- [ ] Needs follow-up analysis before C2.29B.
- [ ] Requires explicit RLS/view/sanitizer decision before continuing.

Recommended next step:

```text

```
