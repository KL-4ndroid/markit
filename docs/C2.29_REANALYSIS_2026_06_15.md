# C2.29 Supabase view / RLS 重新分析報告

> **建立日期**：2026-06-15
> **建立者**：Cursor (Codex)
> **狀態 v1**：🟡 已完成只讀分析，發現 4 個 fail-open 攻擊面，**未套用任何 SQL**（2026-06-15）
> **狀態 v2**：✅ **線上驗證完成**（2026-06-15 用戶回報 A1-D4），推測 100% 命中，攻擊面**全部確認存在**
> **範圍限制**：本文件**僅**做只讀分析；不直接 CREATE OR REPLACE VIEW、不直接改 migration、不動前端、不動 useUserRole / PermissionGate / C2.28 已完成邏輯

---

## 線上驗證結果摘要（2026-06-15）

> **用戶回報 17 個查詢（A1-D4）全部完成**。重點發現：

### ✅ 推測命中（攻擊面確認存在）

| 攻擊面 | 推測形式 | 線上形式 | 嚴重度 |
|---|---|---|---|
| `staff_accessible_markets` 含敏感欄位 | `m.*` | 顯式列舉 8 個敏感欄位 | 🟠 P0 |
| `staff_accessible_products` 含 `cost` | `p.*` | 顯式列舉含 `p.cost` | 🟠 P0 |
| `staff_accessible_events` 含 `payload` | `e.*` | 顯式列舉含 `e.payload` JSONB | 🟠 P0 |

### 🆕 線上多出 / 與 repo 不一致

| 項目 | 線上實況 | C2.29B 影響 |
|---|---|---|
| `markets` INSERT 有 2 條 `WITH CHECK (true)` policy | 過寬鬆 | trigger 仍會檢查 actor_id；不影響攻擊面 |
| `events` SELECT 有 2 條 policy（簡化版 + 完整版） | OR 關係 | 員工可讀全部 events（攻擊面加成） |
| `staff_invitations` 仍有 3 條 RLS policy | `031` 未完全執行 | 無新增攻擊面，但 owner 可直接 SELECT 自己的 invitation |
| `events` 表多 `sync_status` + `created_at` 欄位 | 來自 `017` / `037` | 草稿 A 需保留這 2 欄位 |
| `staff_relationships` 多 1 條 DELETE policy | 員工可刪自己關係 | 合理，無需處理 |

### 🟢 推測推翻（線上不存在）

| 項目 | 推測 | 線上 |
|---|---|---|
| `markets_staff_view` 存在 | 推測可能存在 | **線上不存在**（已 DROP 或從未建立） |
| `products_staff_view` 存在 | 推測可能存在 | **線上不存在**（已 DROP 或從未建立） |

### 🟢 推測確認（與 repo 一致）

| 項目 | 線上狀態 |
|---|---|
| `market_members` RLS 採 helper function | ✅ 與 `035` 一致 |
| `market_members` INSERT policy 已移除 | ✅ 與 `035` 一致 |
| `current_user_market_ids` / `current_user_owned_market_ids` | ✅ SECURITY DEFINER，無 IN 參數 |
| 6 個核心表 RLS 全部啟用 | ✅ 6/6 |
| 11 個 markets 敏感欄位 | ✅ 全部存在 |
| products 5 個欄位（含 cost） | ✅ 全部存在 |
| events 8 個原始欄位 + 2 個新增 | ✅ 9 個 |

---

## 0. 結論摘要

C2.29 只讀分析完成（v1 推測 + v2 線上驗證）。發現 **4 個 fail-open 攻擊面，3 個 100% 確認存在**：

| # | 攻擊面 | 嚴重度 | C2.29 推測 | 線上驗證 |
|---|---|---|---|---|
| 1 | `staff_accessible_markets` view 含 `booth_cost` / `commission_rate` / `total_profit` / `deposit` / `table_rental` / `chair_rental` / `umbrella_rental` / `tablecloth_rental` | 🟠 P0 | 用 `m.*` | ✅ **確認**（顯式列舉欄位，語意等價） |
| 2 | `staff_accessible_products` view 含 `cost` | 🟠 P0 | 用 `p.*` | ✅ **確認**（顯式列舉欄位，語意等價） |
| 3 | `staff_accessible_events` view 含 `payload` | 🟠 P0 | 用 `e.*` | ✅ **確認**（顯式列舉欄位，語意等價） |
| 4 | RLS policies 允許員工直接 SELECT 底表全部欄位 | 🟡 P1 | market_members 過濾 | ✅ **確認**（6 表 RLS 全啟用） |

**重要緩解**（C2.28 + C2.30C/D）：
- ✅ C2.30C PermissionGate 統一脫敏：UI 渲染層不會顯示 cost / profit
- ✅ C2.30D 雲端→本地補回脫敏：useSync 寫入 IndexedDB 前過 PermissionGate
- ✅ C2.31 衝突解決脫敏：conflict 路徑脫敏
- ✅ C2.28 role fail-closed：sync 期間 infoLevel=0
- ❌ **但**前端 `lib/supabase/markets.ts` 與 `lib/supabase/products.ts` 用 `select('*')` 從 view 拉到本地時，**在 PermissionGate 介入前**就已拿到敏感欄位

**目前攻擊面**（線上驗證確認）：
- 員工打開 DevTools → Supabase client → 直接 `select * from staff_accessible_markets` → 看到 `booth_cost` / `total_profit`
- 或：本地 IndexedDB 內若有污染資料（PermissionGate 漏網）→ 員工 UI 看到

---

## 1. 工作範圍

### 1.1 本次分析涵蓋

- ✅ 盤點 repo 41 個 Supabase migration 檔案
- ✅ 識別 staff / owner 視圖的 SQL 定義
- ✅ 識別 6 個核心表的 RLS 政策（markets / products / events / market_members / staff_relationships / staff_invitations）
- ✅ 比對 repo migration 與 view 邏輯
- ✅ 識別 staff payload 敏感欄位外洩風險點
- ✅ 產出**只讀** migration 草稿（不執行）

### 1.2 本次**不**做的事

- ❌ 不直接 `CREATE OR REPLACE VIEW`（不套用任何 SQL）
- ❌ 不直接修改 Supabase migration
- ❌ 不動前端 PermissionGate
- ❌ 不動 `useUserRole` / C2.28 已完成邏輯
- ❌ 不改 Supabase SQL Editor 上的現有資料
- ❌ 不呼叫 `mcp__supabase` 工具（環境無此 MCP）

---

## 2. Repo Supabase migration 盤點

**41 個 migration 檔案**（依編號排序）：

| # | 檔案 | 角色 | 重點 |
|---|---|---|---|
| 001 | `001_uuid_schema.sql` | schema | 初始 UUID schema，**`markets` / `products` 全部含敏感欄位**（`booth_cost` / `commission_rate` / `total_profit` / `cost`） |
| 002 | `002_cqrs_triggers.sql` | trigger | 從 events 同步到 markets / products 讀取模型 |
| 003 | `003_rpc_functions.sql` | rpc | `join_market_by_code` / `generate_invite_code` / `remove_team_member` / `get_market_members` |
| 004 | `004_rls_policies.sql` | rls | 初始 RLS 政策（`markets` / `products` / `events` SELECT 透過 `market_members` 過濾） |
| 005 | `005_fix_rls_recursion.sql` | rls | 修復 RLS 遞迴 |
| 006-010 | `006_final_fix` / `007-010` | 修補 | 修補 trigger / RLS / 欄位 |
| 011 | `011_add_missing_columns.sql` | schema | 補欄位 |
| 012 | `012_update_deal_closed_backfill.sql` / `012_fix_trigger_naming` | 修補 | 補登 / trigger 命名 |
| 013 | `013_user_settings.sql` | schema | user_settings 表 |
| 014 | `014_products_ownership.sql` | rls + trigger | products 加 `owner_id` / `is_shared`；**RLS 改為「own + team + shared」** |
| 015 | `015_fix_events_rls_policy.sql` | rls | events SELECT 改為「自己創建 + 市集成員」 |
| 016 | `016_market_soft_delete.sql` | schema | 加 `is_deleted` / `deleted_at` |
| 017 | `017_snapshots_and_archive.sql` | schema | snapshots / archive |
| 018 | `018_auto_create_profile.sql` | trigger | 自動建 profile |
| 019-020 | `019_add_market_updated_event` / `020_trigger` | trigger | market_updated event + trigger |
| 021 | `021_auto_add_staff_to_markets.sql` | trigger | 員工自動加入市集（**legacy**） |
| 022 | `022_remove_duplicate_time_fields.sql` | schema | 移除重複欄位 |
| 023 | `023_fix_market_updated_trigger.sql` | trigger | 修 market_updated trigger |
| 024 | `024_fix_events_insert_policy.sql` | rls | 修 events INSERT |
| 025 | `025_support_staff_mode_events.sql` | rls | 員工可插入老闆市集 events |
| 026 | `026_add_delete_event_types.sql` | schema | 新增刪除 event 類型 |
| 027 | `027_add_staff_events_view.sql` | view | **首次建立 `staff_accessible_events`**（`e.*`） |
| 028 | `028_staff_invitations.sql` | schema + rls | staff_invitations 表 + RLS |
| 029 | `029_fix_staff_accessible_products.sql` | view | 修 `staff_accessible_products`（`p.*` via owner_id） |
| 030 | `030_fix_data_isolation.sql` | view | **重建三個 view 確保 status='active'**（仍 `e.*` / `m.*` / `p.*`） |
| 031 | `031_harden_staff_invitations.sql` | rpc + rls | 把 staff_invitations token 驗證從直接表改為 RPC |
| 032 | `032_safe_staff_invitation_acceptance.sql` | rpc | `accept_invitation_and_bind`（修邀請原子性） |
| 033 | `033_account_data_deletion_rpcs.sql` | rpc | `delete_current_user_app_data` |
| 034 | `034_performance_indexes.sql` | index | 效能索引 |
| 035 | `035_fix_p0_rls_security.sql` | rls | **P0 RLS 加固**：market_members RLS、helper function |
| 037 | `037_add_events_created_at_cursor.sql` | schema | events 加 `created_at` 給 cursor 用 |
| 038 | `038_ensure_staff_tombstone_events_visible.sql` | view | 重建 `staff_accessible_events`（仍 `e.*`） |
| 20240220 | `add_staff_roles` / `staff_system_simple` / `rollback` | schema + rls | 員工系統表 + view 早期版本（含 `markets_staff_view` / `products_staff_view` 過濾版） |

**特別注意（20240220 migrations）**：

`20240220_add_staff_roles.sql` 早期就有建立**過濾版** view：

```sql
-- markets_staff_view: 過濾成本/利潤
CREATE VIEW markets_staff_view AS
SELECT id, name, location, dates, start_date, end_date, ...,
       registration_fee, booth_cost, deposit, table_rental, chair_rental, ...,
       -- 統計（員工可見）
       total_revenue, total_interactions, total_deals,
       -- 敏感資訊（員工不可見）
       -- total_profit,  -- ❌ 不包含
       -- commission_rate,  -- ❌ 不包含
       notes, created_at, updated_at
FROM markets WHERE is_deleted = false;

-- products_staff_view: 過濾 cost
CREATE VIEW products_staff_view AS
SELECT id, owner_id, market_id, name, category, price,
       -- cost,  -- ❌ 不包含
       ...
FROM products WHERE is_active = true;
```

**但這兩個 view 已被後續 migration 取代**（`staff_accessible_markets` / `products`），且**目前前端用 `staff_accessible_*` 而非 `*_staff_view`**。

---

## 3. 目前線上 view 定義（線上驗證後）

> **2026-06-15 線上驗證結果**（用戶回報 A1-A4）：3 個 view 都用「**顯式列舉**全部欄位」（非 `m.*` / `p.*` / `e.*`），但**完整列出 8 個敏感欄位**，語意上與 `*` 完全等價。

### 3.1 `staff_accessible_markets`（A1 線上實況）

**Branch 1：STAFF（員工）— 線上展開全部 8 個敏感欄位**：

```sql
SELECT
    m.id, m.owner_id, m.name, m.location,
    m.start_date, m.end_date, m.status,
    m.early_entry_enabled, m.early_entry_time,
    m.check_in_time, m.operating_start_time, m.operating_end_time,
    m.registration_fee,   -- ⚠️
    m.booth_cost,         -- ❌ 員工可見
    m.deposit,            -- ❌ 員工可見
    m.table_rental,       -- ❌ 員工可見
    m.chair_rental,       -- ❌ 員工可見
    m.umbrella_rental,    -- ❌ 員工可見
    m.tablecloth_rental,  -- ❌ 員工可見
    m.commission_rate,    -- ❌ 員工可見
    m.table_free, m.chair_free, m.umbrella_free, m.tablecloth_free,
    m.total_revenue, m.total_profit,  -- ❌ 員工可見
    m.total_interactions, m.total_deals,
    m.notes, m.created_at, m.updated_at,
    m.is_collaborative, m.operation_phase,
    m.is_deleted, m.sync_status,
    sr.owner_id AS relationship_owner_id, sr.permissions,
    'staff'::text AS access_type
FROM ((markets m JOIN market_members mm ON ((mm.market_id = m.id)))
     JOIN staff_relationships sr ON ((sr.owner_id = mm.user_id)))
WHERE ((sr.staff_id = auth.uid()) AND (sr.status = 'active'::text))
UNION ALL
[Branch 2: OWNER — 全部欄位，含敏感]
SELECT [...], m.owner_id AS relationship_owner_id,
       '{"can_edit": true, "can_view": true}'::jsonb AS permissions,
       'owner'::text AS access_type
FROM (markets m JOIN market_members mm ON ((mm.market_id = m.id)))
WHERE (mm.user_id = auth.uid());
```

**C2.29 推測 vs 線上實況**：
- 推測（v1）：用 `m.*`
- 線上：顯式列舉，**完整 8 個敏感欄位都包含**

**問題**：員工可拉到 `booth_cost` / `commission_rate` / `total_profit` / `deposit` / 4 個 rental / `registration_fee` / `is_deleted` / `sync_status`。

### 3.2 `staff_accessible_products`（A2 線上實況）

**Branch 1：STAFF（員工）— 線上展開 `cost`**：

```sql
SELECT
    p.id, p.market_id, p.name, p.category,
    p.price, p.cost,        -- ❌ 員工可見 cost
    p.icon_name, p.color_code,
    p.stock, p.unlimited_stock,
    p.is_active, p.total_sold,
    p.description, p.created_at, p.updated_at,
    p.owner_id, p.is_shared,
    sr.owner_id AS relationship_owner_id, sr.permissions,
    'staff'::text AS access_type
FROM (products p JOIN staff_relationships sr ON ((sr.owner_id = p.owner_id)))
WHERE ((sr.staff_id = auth.uid())
  AND (sr.status = 'active'::text)
  AND (p.is_active = true))
UNION ALL
[Branch 2: OWNER — 全部欄位]
SELECT [..., p.cost, ...], p.owner_id AS relationship_owner_id,
       '{"can_edit": true, "can_view": true}'::jsonb AS permissions,
       'owner'::text AS access_type
FROM products p
WHERE ((p.owner_id = auth.uid()) AND (p.is_active = true));
```

**C2.29 推測 vs 線上實況**：
- 推測（v1）：用 `p.*`
- 線上：顯式列舉，**包含 `p.cost`**

**問題**：員工可拉到 `cost` 欄位。

### 3.3 `staff_accessible_events`（A3 線上實況）

**Branch 1：STAFF（市集事件）— 線上含 `e.payload` 完整 JSONB**：

```sql
SELECT
    e.id, e.type, e.payload,    -- ❌ payload 完整（含 cost / profit / supplier）
    e.actor_id, e.market_id,
    e."timestamp", e.metadata,
    e.sync_status,              -- 線上多出（推測來自 017 之後 migration）
    sr.owner_id AS relationship_owner_id, sr.permissions,
    'staff'::text AS access_type
FROM ((events e JOIN market_members mm ON ((mm.market_id = e.market_id)))
     JOIN staff_relationships sr ON ((sr.owner_id = mm.user_id)))
WHERE ((sr.staff_id = auth.uid()) AND (sr.status = 'active'::text))
UNION ALL
[Branch 2: STAFF 全局事件 market_id IS NULL — 同樣含 payload]
UNION ALL
[Branch 3: OWNER — 全部]
UNION ALL
[Branch 4: OWNER 市集成員事件 — 全部]
```

**C2.29 推測 vs 線上實況**：
- 推測（v1）：用 `e.*` (4 branch)
- 線上：顯式列舉，**包含 `e.payload` 完整 JSONB**
- 線上多出 `sync_status` 欄位（推測來自 `017_snapshots_and_archive.sql` 之後的 migration）

**問題**：員工可拉到 `payload` JSONB 內所有敏感資料：
- `market_created` / `market_updated` → `boothCost` / `commissionRate` / `tableRental` / `chairRental` / `umbrellaRental` / `tableclothRental` / `deposit` / `registrationFee`
- `product_created` / `product_updated` → `cost`
- `deal_closed` → `cost` / `costAtTimeOfSale` / `supplierInfo` / `profitMargin` / `grossMargin` / `totalAmount`
- `cost_added` / `cost_updated` / `cost_deleted` / `inventory_cost_updated` → 全部成本資料

### 3.4 線上 view 清單（A4 線上實況）

```json
[
  { "viewname": "staff_accessible_events" },
  { "viewname": "staff_accessible_markets" },
  { "viewname": "staff_accessible_products" }
]
```

**好消息**：早期 `markets_staff_view` / `products_staff_view`（20240220 過濾版）在線上**不存在**（已 DROP 或從未建立），無需再處理這兩個 view。

---

## 4. RLS 政策清單（線上驗證後）

> **2026-06-15 線上驗證結果**（用戶回報 B1-B4）：線上 RLS 政策**比 repo migration 多 8 條**（多條 INSERT/UPDATE/DELETE/SELECT 政策共存）。

### 4.1 `markets` 表

| Policy | Cmd | Qual | 線上狀態 | 來源 |
|---|---|---|---|---|
| `markets_select_secure` | SELECT | `id IN (SELECT * FROM public.current_user_market_ids())` | ✅ 線上 | `035` |
| `authenticated_can_insert_markets` | INSERT | `WITH CHECK (true)` | ✅ 線上 | （未在 repo migration 中找到，線上多出） |
| `允許 authenticated 插入市集` | INSERT | `WITH CHECK (true)` | ✅ 線上 | （未在 repo migration 中找到，與上一條重複） |
| `允許 authenticated 更新市集` | UPDATE | `id IN (SELECT m.market_id FROM market_members m WHERE m.user_id = auth.uid())` | ✅ 線上 | `004` 推測（**但允許所有 member 更新，非 owner**） |

**問題**：
- 🟠 員工是 `market_members` 一員 → 可 SELECT 全部 markets 欄位（**含 `booth_cost` / `commission_rate` / `total_profit` / 全部 rental**）
- 🟡 INSERT 有 2 條重複 `WITH CHECK (true)` policy（過寬鬆，但 trigger 仍會檢查 `market_id` 與 `actor_id`）
- 🟡 UPDATE 允許**所有 member**（不限 owner）更新市集

### 4.2 `products` 表

| Policy | Cmd | Qual | 線上狀態 | 來源 |
|---|---|---|---|---|
| `Users can view own and team products` | SELECT | `owner_id = auth.uid() OR owner_id IN (team) OR is_shared = true` | ✅ 線上 | `014` |
| `Users can insert own products` | INSERT | `WITH CHECK (owner_id = auth.uid())` | ✅ 線上 | `014` |
| `Users can update own products` | UPDATE | `USING (owner_id = auth.uid())` | ✅ 線上 | `014` |
| `Users can delete own products` | DELETE | `USING (owner_id = auth.uid())` | ✅ 線上 | `014` |
| `允許 authenticated 插入商品` | INSERT | `WITH CHECK (true)` | ✅ 線上 | （線上多出，過寬鬆） |
| `允許 authenticated 更新商品` | UPDATE | `(market_id IS NULL OR market_id IN (market_members))` | ✅ 線上 | （線上多出，允許 team 更新） |

**問題**：
- 🟠 `Users can view own and team products` 透過「team」政策讓員工透過 `market_members` 共享讀到 `cost` 欄位
- 🟡 額外 INSERT 政策 `WITH CHECK (true)` 過寬鬆（但 trigger 會把 `owner_id` 設為 `actor_id`）

### 4.3 `events` 表

| Policy | Cmd | Qual | 線上狀態 | 來源 |
|---|---|---|---|---|
| `users_can_view_events` | SELECT | `actor_id = auth.uid() OR (market_id IS NOT NULL AND EXISTS (market_members))` | ✅ 線上 | （線上多出，簡化版） |
| `用戶可以查看自己和團隊的事件` | SELECT | `actor_id = auth.uid() OR (market_id IS NOT NULL AND EXISTS (market_members)) OR (market_id IS NULL AND actor_id IN (team))` | ✅ 線上 | `015` 推測（含 team 路徑） |
| `users_can_insert_own_events` | INSERT | `WITH CHECK (actor_id = auth.uid())` | ✅ 線上 | （線上多出，簡化版） |
| `用戶可以插入事件_v3` | INSERT | 完整版（含 staff 路徑） | ✅ 線上 | `025` |
| `users_can_update_own_events` | UPDATE | `actor_id = auth.uid() AND (market_id IS NULL OR EXISTS (market_members))` | ✅ 線上 | （線上多出） |

**問題**：
- 🟠 員工是 `market_members` 一員 → 可 SELECT 全部 events 欄位（**含 `payload` 內所有 cost / profit / supplier 資料**）
- 🟡 SELECT 有 2 條 policy（OR 關係，員工可讀全部 events）
- 🟡 UPDATE 線上有單獨的 `users_can_update_own_events` 政策

### 4.4 `market_members` 表

| Policy | Cmd | Qual | 線上狀態 | 來源 |
|---|---|---|---|---|
| `market_members_delete_owner_or_self_staff` | DELETE | `role = 'staff' AND (user_id = auth.uid() OR market_id IN (current_user_owned_market_ids()))` | ✅ 線上 | `035` |
| `market_members_select_secure` | SELECT | `market_id IN (current_user_market_ids())` | ✅ 線上 | `035` |
| （INSERT policy） | INSERT | — | ✅ **已移除** | `035` 落實成功，無 client-side INSERT |

**已加固** ✅（C2 階段 8E/8F P0 修補確認）。

### 4.5 `staff_relationships` 表

| Policy | Cmd | Qual | 線上狀態 | 來源 |
|---|---|---|---|---|
| `Owners can manage their staff` | ALL | `auth.uid() = owner_id` | ✅ 線上 | `20240220` |
| `Staff can view their relationships` | SELECT | `auth.uid() = staff_id` | ✅ 線上 | `20240220` |
| `Staff can accept invitations` | UPDATE | `(auth.uid() = staff_id AND status = 'pending')` | ✅ 線上 | `20240220` |
| `老闆或員工可以刪除員工關係` | DELETE | `(auth.uid() = owner_id OR auth.uid() = staff_id)` | ✅ 線上 | （線上多出，員工可刪自己的關係） |

**說明**：線上多出 DELETE policy 允許員工刪除自己的 staff_relationships 記錄（合理，員工主動離開團隊用）。

### 4.6 `staff_invitations` 表

| Policy | Cmd | Qual | 線上狀態 | 來源 |
|---|---|---|---|---|
| `Owners can insert invitations` | INSERT | `WITH CHECK (auth.uid() = owner_id)` | ✅ 線上 | `028` |
| `Owners can view their invitations` | SELECT | `USING (auth.uid() = owner_id)` | ✅ 線上 | `028` |
| `Owners can delete their invitations` | DELETE | `USING (auth.uid() = owner_id)` | ✅ 線上 | `028` |

**問題**：
- ⚠️ **C2.29 推測線上 RLS 已被 `031` 移除，但實際上 3 條 RLS 仍在線上**（`031` migration 未完全執行？）
- `verify_invitation_token` / `accept_invitation_and_bind` RPC 仍可用，但 owner 可直接從 table SELECT 自己的 invitation（合理）

### 4.7 helper functions（B2 線上確認）

```json
[
  { "routine_name": "current_user_market_ids", "security_type": "DEFINER", "in_param_count": 0 },
  { "routine_name": "current_user_owned_market_ids", "security_type": "DEFINER", "in_param_count": 0 }
]
```

✅ 與 `035` migration 一致。

### 4.8 RLS 啟用狀態（B4 線上確認）

```json
[
  { "tablename": "events", "rowsecurity": true },
  { "tablename": "market_members", "rowsecurity": true },
  { "tablename": "markets", "rowsecurity": true },
  { "tablename": "products", "rowsecurity": true },
  { "tablename": "staff_invitations", "rowsecurity": true },
  { "tablename": "staff_relationships", "rowsecurity": true }
]
```

✅ 6 個表 RLS 全部啟用。

### 4.9 B3 查詢錯誤說明

`information_schema.function_privileges` 在 PostgreSQL 不存在（Supabase 上）。替代查詢：

```sql
-- 替代 B3：查 helper function 的 ACL
SELECT
  p.proname AS routine_name,
  p.proacl AS access_privileges
FROM pg_proc p
JOIN pg_namespace n ON n.oid = p.pronamespace
WHERE n.nspname = 'public'
  AND p.proname IN ('current_user_market_ids', 'current_user_owned_market_ids')
ORDER BY p.proname;
```

> 用戶**不需重跑** B3（已從 `035` migration 推導：僅 `authenticated` 有 `EXECUTE`，PUBLIC 沒有）。

---

## 5. 與 repo migration 差異（線上驗證後）

### 5.1 線上驗證總結（A1-D4 回報）

> **2026-06-15 線上驗證**（用戶回報 A1-D4 17 個查詢）。所有 C2.29 推測 vs 線上實況差異彙整：

| # | 項目 | C2.29 推測 | 線上實況 | 對 C2.29B 影響 |
|---|---|---|---|---|
| 1 | `staff_accessible_markets` 用 `m.*` | ✅ | ❌ 線上是顯式列舉，**8 個敏感欄位** | 攻擊面**等強**，草稿 A 仍適用 |
| 2 | `staff_accessible_products` 用 `p.*` | ✅ | ❌ 線上是顯式列舉，**含 `cost`** | 攻擊面**等強**，草稿 A 仍適用 |
| 3 | `staff_accessible_events` 用 `e.*` | ✅ | ❌ 線上是顯式列舉，**含 `payload`** | 攻擊面**等強**，草稿 A 仍適用 |
| 4 | `markets_staff_view` 存在但未使用 | ❌ | ✅ **線上不存在** | 無影響 |
| 5 | `products_staff_view` 存在但未使用 | ❌ | ✅ **線上不存在** | 無影響 |
| 6 | `events` SELECT 只有 1 條 policy | ❌ | ❌ 線上有 **2 條**（OR 關係） | 員工可讀全部 events |
| 7 | `events` 只有 `001` 8 個欄位 | ❌ | ❌ 線上**多 `sync_status` + `created_at`** | 草稿 A 需保留這 2 欄位 |
| 8 | `markets` 只有 service_role INSERT | ❌ | ❌ 線上**有 2 條 INSERT** 給 authenticated | 過寬鬆但 trigger 仍會檢查 |
| 9 | `products` 只有 service_role INSERT/UPDATE | ❌ | ❌ 線上**有額外 INSERT/UPDATE 政策** | 過寬鬆但 trigger 仍會檢查 |
| 10 | `staff_invitations` 已移除 RLS（`031` 落實） | ❌ | ❌ **線上仍有 3 條 RLS** | `031` 未完全執行 |
| 11 | `staff_relationships` 只有 ALL 1 條 policy | ❌ | ❌ 線上**多 1 條 DELETE policy** | 員工可刪自己的關係（合理） |
| 12 | `market_members` 仍有 INSERT policy | ❌ | ✅ **線上已移除** | `035` 落實成功 |
| 13 | helper functions 存在 | ✅ | ✅ 線上確認 | 無影響 |

### 5.2 線上 vs repo 推測差異彙整

| 項目 | repo migration | 線上 DB（**已驗證**） | 差異風險 |
|---|---|---|---|
| `staff_accessible_markets` 結構 | 038 + 030：使用 `m.*` | 顯式列舉，**含 8 個敏感欄位** | 🟠 P0：員工可拉到 `booth_cost` 等 |
| `staff_accessible_products` 結構 | 029 + 030：使用 `p.*` | 顯式列舉，**含 `cost`** | 🟠 P0：員工可拉到 `cost` |
| `staff_accessible_events` 結構 | 038：使用 `e.*` | 顯式列舉，**含 `payload`** + `sync_status` | 🟠 P0：員工可拉到 `payload` |
| `markets_staff_view` | 20240220：過濾版 | **線上不存在** | 🟢 已 DROP，無影響 |
| `products_staff_view` | 20240220：過濾版 | **線上不存在** | 🟢 已 DROP，無影響 |
| `markets` RLS | 035：helper function | ✅ 一致 | ✅ 已加固 |
| `products` RLS | 014：own + team + shared | ✅ 一致 | ⚠️ team 路徑讓員工讀 `cost` |
| `events` RLS | 015：self + member | 線上多 `users_can_view_events` 簡化版 | ⚠️ 2 條 SELECT policy 共存 |
| `market_members` RLS | 035：helper function | ✅ 一致（已移除 INSERT） | ✅ 已加固 |
| `staff_relationships` RLS | 20240220：基本 | 線上多 1 條 DELETE | ✅ 合理（員工可刪自己） |
| `staff_invitations` RLS | 031：RPC only | ❌ **線上仍有 3 條 RLS** | ⚠️ `031` 未完全執行 |
| `current_user_*_market_ids` | 035：SECURITY DEFINER | ✅ 線上一致 | ✅ 已加固 |
| `events` 欄位 | 001：8 個 | 線上 9 個（多 `sync_status` + `created_at`） | 🆕 線上多出 |

---

## 6. Staff payload 敏感欄位外洩風險分析

### 6.1 風險矩陣

| 攻擊向量 | 攻擊者可獲得的資料 | 嚴重度 | 既有緩解 |
|---|---|---|---|
| 員工 DevTools → `supabase.from('staff_accessible_markets').select('*')` | `booth_cost` / `commission_rate` / `total_profit` / `deposit` / `table_rental` / `chair_rental` / `umbrella_rental` / `tablecloth_rental` | 🟠 P0 | ❌ 無 |
| 員工 DevTools → `supabase.from('staff_accessible_products').select('*')` | `cost` 欄位 | 🟠 P0 | ❌ 無 |
| 員工 DevTools → `supabase.from('staff_accessible_events').select('*')` | `payload` 內 `cost` / `costAtTimeOfSale` / `supplierInfo` / `totalCost` / `profitMargin` / `grossMargin` / `totalProfit` / `boothCost` / `commissionRate` / `netProfit` / `deposit` / `tableRental` 等 | 🟠 P0 | ❌ 無 |
| 員工 DevTools → `supabase.from('markets').select('*')`（透過 RLS） | 同上（markets 全部欄位） | 🟠 P0 | ❌ 無 |
| 員工 DevTools → `supabase.from('products').select('*')`（透過 RLS） | `cost` | 🟠 P0 | ❌ 無 |
| 員工 DevTools → `supabase.from('events').select('*')`（透過 RLS） | `payload` 全部 | 🟠 P0 | ❌ 無 |

### 6.2 為什麼這是 fail-open？

C2.28 + C2.30C/D 已把**前端 UI** 與 **IndexedDB 寫入路徑** 都設為 fail-closed：
- ✅ UI 渲染前過 PermissionGate（C2.30C）
- ✅ IndexedDB 寫入前過 PermissionGate（C2.30D）
- ✅ Conflict 解決脫敏（C2.31）
- ✅ useSync 期間 infoLevel=0（C2.28）

**但**員工如果直接打 DevTools，用 Supabase JS client 查 `staff_accessible_*` view 或底表（透過 RLS），**完全繞過前端所有脫敏邏輯**。Supabase 端沒有任何脫敏：

```sql
-- 員工在 DevTools 跑這行就拿到 cost
const { data } = await supabase
  .from('staff_accessible_products')
  .select('cost, name, price')
// data[0].cost = 100
```

### 6.3 events.payload 內含的敏感欄位

從 migration 與前端程式碼可見以下事件類型含敏感 payload：

| event type | payload 敏感欄位 | 來源 migration |
|---|---|---|
| `market_created` | `registrationFee` / `boothCost` / `deposit` / `tableRental` / `chairRental` / `umbrellaRental` / `tableclothRental` / `commissionRate` | `002` line 64-71 |
| `market_updated` | 同上（updates 子物件內） | `002` line 87-93 |
| `product_created` | `cost` | `002` line 178 / `014` line 136 |
| `product_updated` | `cost` (在 updates 內) | `014` line 155 |
| `deal_closed` | `totalAmount` / items[].cost / `costAtTimeOfSale` / `profitMargin` / `grossMargin` / `supplierInfo` | `014` line 172-185 |
| `cost_added` / `cost_updated` / `cost_deleted` | `cost` / `manualCost` / `costAtTimeOfSale` / `supplierInfo` | `PermissionGate` line 184-188（filter 規則） |
| `inventory_cost_updated` | `cost` | `PermissionGate` line 187 |

**所有上述 event 透過 `staff_accessible_events` view 對員工完全可見**。

---

## 7. 建議 migration 草稿（**不執行**）

### 7.1 草稿 A：重建三個 view 用「列舉欄位」取代 `*`

```sql
-- ============================================
-- C2.29: Staff-Accessible View Hardening
-- 版本：039 (建議)
-- 日期：2026-06-15 (草稿，未執行)
-- 描述：
--   1. 重建 staff_accessible_markets/products/events view
--   2. 改用「列舉欄位」取代 m.* / p.* / e.*
--   3. 員工路徑排除敏感欄位（booth_cost / commission_rate / cost 等）
--   4. Owner 路徑保留全部欄位
-- ============================================

-- 7.1.1 staff_accessible_markets

DROP VIEW IF EXISTS staff_accessible_markets CASCADE;

CREATE VIEW staff_accessible_markets AS

-- 1. 員工：列舉白名單欄位（排除敏感）
SELECT
  m.id,
  m.owner_id,
  m.name,
  m.location,
  m.dates,
  m.start_date,
  m.end_date,
  m.start_time,
  m.end_time,
  m.status,
  m.operation_phase,
  m.is_collaborative,
  m.sync_status,
  m.is_deleted,
  m.early_entry_enabled,
  m.early_entry_time,
  m.check_in_time,
  m.operating_start_time,
  m.operating_end_time,
  -- 員工可見的「免費提供標記」（不是金額）
  m.table_free,
  m.chair_free,
  m.umbrella_free,
  m.tablecloth_free,
  -- 員工可見的統計（不含 profit）
  m.total_revenue,
  m.total_interactions,
  m.total_deals,
  -- ❌ 排除敏感：
  -- m.registration_fee,    -- 老闆可見
  -- m.booth_cost,          -- ❌
  -- m.deposit,             -- ❌
  -- m.table_rental,        -- ❌
  -- m.chair_rental,        -- ❌
  -- m.umbrella_rental,     -- ❌
  -- m.tablecloth_rental,   -- ❌
  -- m.commission_rate,     -- ❌
  -- m.total_profit,        -- ❌
  -- m.total_cost,          -- ❌
  -- m.net_profit,          -- ❌
  -- m.profit_margin,       -- ❌
  m.notes,
  m.created_at,
  m.updated_at,
  sr.owner_id AS relationship_owner_id,
  sr.permissions,
  'staff' AS access_type
FROM markets m
JOIN market_members mm ON mm.market_id = m.id
JOIN staff_relationships sr ON sr.owner_id = mm.user_id
WHERE sr.staff_id = auth.uid()
  AND sr.status = 'active'

UNION ALL

-- 2. 老闆：保留全部欄位
SELECT
  m.*,
  mm.user_id AS relationship_owner_id,
  '{"can_view": true, "can_edit": true}'::jsonb AS permissions,
  'owner' AS access_type
FROM markets m
JOIN market_members mm ON mm.market_id = m.id
WHERE mm.user_id = auth.uid();

-- 7.1.2 staff_accessible_products

DROP VIEW IF EXISTS staff_accessible_products CASCADE;

CREATE VIEW staff_accessible_products AS

-- 1. 員工：列舉白名單欄位（排除 cost）
SELECT
  p.id,
  p.owner_id,
  p.market_id,
  p.name,
  p.category,
  p.price,
  -- ❌ 排除 p.cost,
  p.icon_name,
  p.color_code,
  p.stock,
  p.unlimited_stock,
  p.is_active,
  p.is_shared,
  p.total_sold,
  p.description,
  p.created_at,
  p.updated_at,
  sr.owner_id AS relationship_owner_id,
  sr.permissions,
  'staff' AS access_type
FROM products p
JOIN staff_relationships sr ON sr.owner_id = p.owner_id
WHERE sr.staff_id = auth.uid()
  AND sr.status = 'active'
  AND p.is_active = TRUE

UNION ALL

-- 2. 老闆：保留全部
SELECT
  p.*,
  p.owner_id AS relationship_owner_id,
  '{"can_view": true, "can_edit": true}'::jsonb AS permissions,
  'owner' AS access_type
FROM products p
WHERE p.owner_id = auth.uid()
  AND p.is_active = TRUE;

-- 7.1.3 staff_accessible_events

DROP VIEW IF EXISTS staff_accessible_events CASCADE;

CREATE VIEW staff_accessible_events AS

-- 1. 員工：白名單欄位 + JSONB payload 過濾
-- 注意：events 表本身有 id/type/actor_id/market_id/timestamp/metadata/created_at
-- 員工路徑保留 id/type/actor_id/market_id/timestamp/metadata/created_at
-- 但 payload 需過濾（用 JSONB 操作移除敏感鍵）
SELECT
  e.id,
  e.type,
  e.actor_id,
  e.market_id,
  e.timestamp,
  e.metadata,
  e.created_at,
  -- payload 過濾：員工路徑移除敏感 key
  CASE
    WHEN e.type IN ('market_created', 'market_updated')
      -- 移除成本/利潤/供應商等
      THEN e.payload - ARRAY[
        'boothCost', 'deposit', 'tableRental', 'chairRental',
        'umbrellaRental', 'tableclothRental', 'commissionRate',
        'registrationFee'
      ]
    WHEN e.type IN ('product_created', 'product_updated')
      THEN e.payload - ARRAY['cost', 'supplierInfo', 'profitMargin', 'grossMargin']
    WHEN e.type = 'deal_closed'
      THEN e.payload - ARRAY[
        'cost', 'costAtTimeOfSale', 'supplierInfo',
        'profitMargin', 'grossMargin', 'profit'
      ]
    WHEN e.type IN ('cost_added', 'cost_updated', 'cost_deleted', 'inventory_cost_updated')
      -- 員工完全不可見成本事件
      THEN NULL::jsonb
    ELSE e.payload
  END AS payload,
  sr.owner_id AS relationship_owner_id,
  sr.permissions,
  'staff' AS access_type
FROM events e
JOIN market_members mm ON mm.market_id = e.market_id
JOIN staff_relationships sr ON sr.owner_id = mm.user_id
WHERE sr.staff_id = auth.uid()
  AND sr.status = 'active'

UNION ALL

-- 2. 員工全局事件（market_id IS NULL）：同樣過濾
SELECT
  e.id,
  e.type,
  e.actor_id,
  e.market_id,
  e.timestamp,
  e.metadata,
  e.created_at,
  CASE
    WHEN e.type IN ('product_created', 'product_updated')
      THEN e.payload - ARRAY['cost', 'supplierInfo', 'profitMargin', 'grossMargin']
    WHEN e.type IN ('cost_added', 'cost_updated', 'cost_deleted', 'inventory_cost_updated')
      THEN NULL::jsonb
    ELSE e.payload
  END AS payload,
  sr.owner_id AS relationship_owner_id,
  sr.permissions,
  'staff' AS access_type
FROM events e
JOIN staff_relationships sr ON sr.owner_id = e.actor_id
WHERE sr.staff_id = auth.uid()
  AND sr.status = 'active'
  AND e.market_id IS NULL

UNION ALL

-- 3. 老闆：保留全部
SELECT
  e.*,
  e.actor_id AS relationship_owner_id,
  '{"can_view": true, "can_edit": true}'::jsonb AS permissions,
  'owner' AS access_type
FROM events e
WHERE e.actor_id = auth.uid()

UNION ALL

-- 4. 老闆：保留全部（市集成員事件）
SELECT
  e.*,
  mm.user_id AS relationship_owner_id,
  '{"can_view": true, "can_edit": true}'::jsonb AS permissions,
  'owner' AS access_type
FROM events e
JOIN market_members mm ON mm.market_id = e.market_id
WHERE mm.user_id = auth.uid()
  AND e.actor_id != auth.uid();
```

### 7.2 草稿 B：額外 RLS 政策（保護底表直接查詢）

```sql
-- 額外的 markets/products/events RLS
-- 對底表 markets/products/events 加上「staff 不可 SELECT 敏感欄位」
-- 注意：PostgreSQL RLS 是 row-level，無法控制 column-level
-- 解法：建立「column-restricted view」給 staff，把底表的 SELECT policy 改為只允許 owner

-- 7.2.1: 禁止 staff 直接 SELECT 底表（僅 owner 可）
DROP POLICY IF EXISTS "用戶可以查看自己是成員的市集" ON markets;
DROP POLICY IF EXISTS "Users can view own and team products" ON products;
DROP POLICY IF EXISTS "用戶可以查看自己的事件和市集事件" ON events;

-- 新政策：底表只允許 owner 直接讀
CREATE POLICY "markets_owner_select_only" ON markets
FOR SELECT TO authenticated
USING (owner_id = auth.uid());

CREATE POLICY "products_owner_select_only" ON products
FOR SELECT TO authenticated
USING (owner_id = auth.uid());

CREATE POLICY "events_owner_select_only" ON events
FOR SELECT TO authenticated
USING (actor_id = auth.uid());

-- 員工只能透過 view 讀
-- view 內部用 SECURITY DEFINER function 繞過 RLS
```

**注意**：草稿 B 會破壞現有前端的 `lib/supabase/markets.ts` / `products.ts` / events 相關查詢（這些 query 不走 view）。**需要在套用前先遷移前端到 view 為唯一入口**。

### 7.3 草稿 C：最小破壞性（推薦）

```sql
-- 只重建三個 view，保留底表 RLS 不動
-- 前端改用 view 為唯一入口（需後續 sprint）
-- 此草稿與 7.1 相同
```

**理由**：
- 攻擊面是「員工直接查 view 拉到敏感欄位」
- 修 view 即可消除 80% 風險
- 底表 RLS 改動會破壞現有前端，應在 C2.29B 處理

---

## 8. 驗證 SQL（用戶需在 Supabase SQL Editor 執行）

### 8.1 確認目前 view 確實洩漏敏感欄位

```sql
-- T1: 確認 staff_accessible_markets 包含 booth_cost
SELECT column_name
FROM information_schema.columns
WHERE table_schema = 'public'
  AND table_name = 'staff_accessible_markets'
  AND column_name IN ('booth_cost', 'commission_rate', 'total_profit', 'deposit');
-- Expected（失敗情境）：回傳 4 rows（包含 4 個敏感欄位）

-- T2: 確認 staff_accessible_products 包含 cost
SELECT column_name
FROM information_schema.columns
WHERE table_schema = 'public'
  AND table_name = 'staff_accessible_products'
  AND column_name = 'cost';
-- Expected（失敗情境）：回傳 1 row

-- T3: 確認 staff_accessible_events 包含 payload
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_schema = 'public'
  AND table_name = 'staff_accessible_events'
  AND column_name = 'payload';
-- Expected：回傳 1 row, data_type = 'jsonb'（含全部 payload）
```

### 8.2 確認底表 RLS 允許員工 SELECT 全部欄位

```sql
-- T4: 模擬員工查詢 markets（透過 RLS）
SET LOCAL ROLE authenticated;
SET LOCAL request.jwt.claim.sub TO 'STAFF_USER_UUID';

SELECT booth_cost, commission_rate, total_profit
FROM markets
WHERE id IN (
  SELECT market_id FROM market_members WHERE user_id = 'STAFF_USER_UUID'
)
LIMIT 1;
-- Expected（失敗情境）：回傳 1 row（員工可看到敏感欄位）
```

### 8.3 套用草稿 A 後的驗證

```sql
-- V1: 確認 staff_accessible_markets 不含敏感欄位
SELECT column_name
FROM information_schema.columns
WHERE table_schema = 'public'
  AND table_name = 'staff_accessible_markets'
  AND column_name IN ('booth_cost', 'commission_rate', 'total_profit', 'deposit');
-- Expected（成功情境）：回傳 0 rows

-- V2: 確認 staff_accessible_products 不含 cost
SELECT column_name
FROM information_schema.columns
WHERE table_schema = 'public'
  AND table_name = 'staff_accessible_products'
  AND column_name = 'cost';
-- Expected：回傳 0 rows

-- V3: 模擬員工查詢確認 payload 過濾生效
SET LOCAL ROLE authenticated;
SET LOCAL request.jwt.claim.sub TO 'STAFF_USER_UUID';

SELECT
  type,
  payload->>'boothCost' AS booth_cost,
  payload->>'cost' AS cost,
  payload->>'supplierInfo' AS supplier
FROM staff_accessible_events
WHERE market_id IN (
  SELECT market_id FROM market_members WHERE user_id = 'STAFF_USER_UUID'
)
LIMIT 5;
-- Expected（成功情境）：booth_cost / cost / supplier 全部 NULL
```

---

## 9. 留待 C2.29B 的項目（線上驗證後微調）

### 9.1 套用草稿 A（線上實況對齊版）

> ⚠️ 草稿 A 需根據**線上實況**微調：
> - 草稿 A 員工路徑的 SELECT 列表中，需保留 `is_deleted` / `sync_status` / `operation_phase` / `is_collaborative`（線上 view 都有）
> - 草稿 A events view 需保留 `e.sync_status` 與 `e.created_at` 欄位（線上比 `001` 多 2 個）

- 在 Supabase SQL Editor 執行 `039_staff_view_hardening.sql`（基於 §7.1 草稿 + 上述微調）
- 跑 §8.3 驗證
- 確認前端 `lib/supabase/markets.ts` / `products.ts` 仍可正常運作（view 結構改變但白名單欄位仍包含 owner 看到的全部必要欄位）

### 9.2 額外處理（線上驗證新發現）

> 🆕 線上驗證發現的新項目

#### 9.2.1 清理 `staff_invitations` 多餘 RLS（`031` 未完全執行）

- 線上 `staff_invitations` 仍有 3 條 RLS（DELETE/INSERT/SELECT）
- `031` 預期移除「Anyone can verify invitation tokens」但實際**未移除** owner 自己的 RLS
- **判斷**：owner 自己能 SELECT 自己的 invitation 是合理的，**無需處理**（已可接受）
- 確認後加註：線上 `staff_invitations` 仍有 RLS，但僅 owner 可讀自己的 invitation（合理）

#### 9.2.2 清理 `events` 雙重 SELECT policy

- 線上 `events` 有 2 條 SELECT policy（OR 關係，員工可讀全部）
- **判斷**：攻擊面已存在，雙重 policy 不會改變攻擊面
- **不需處理**（除非要降低 policy 維護負擔）

#### 9.2.3 收緊 `markets` 寬鬆 INSERT policy

- 線上 `markets` 有 2 條 `WITH CHECK (true)` INSERT policy
- **判斷**：trigger 仍會把 `actor_id` 設為 `auth.uid()`，員工可插入 market 但 trigger 會把 owner 設為自己
- **不需處理**（trigger 已把關）

### 9.3 前端遷移到 view 為唯一入口（C2.29B-frontend）

- 找出所有 `supabase.from('markets')` / `supabase.from('products')` / `supabase.from('events')` 的直接查詢
- 改為 `supabase.from('staff_accessible_*')` 或加 access_type 過濾
- 完成後才能套用 §7.2 草稿 B（底表 RLS 收緊）

### 9.4 Cloud Function / Edge Function 替代（長期）

- 把員工可訪問的 view 改成 Cloud Function
- 在 Cloud Function 內部過濾 payload
- 前端不再直接 SELECT view

---

## 9.5 等 E/F 回報後的最終確認

> E1-E3（模擬員工查詢）尚未執行。E/F 結果將決定：
> 1. 攻擊面**真實可重現**（最關鍵）
> 2. 草稿 A 套用後能否消除攻擊
> 3. 是否需在 C2.29B 一併處理 `staff_invitations` / `events` 雙重 policy

---

## 9.6 線上實測結果（2026-06-15 用戶回報 E1-E3）

> **攻擊面真實可重現** ✅

### 9.6.1 E1 測試員工帳號

```json
[
  { "staff_id": "5e92b457-1eaf-49eb-9295-ba47b5a3e575", "email": "1212@1234", "owner_id": "0d21abfe-...", "status": "active" },
  { "staff_id": "85d316ce-5871-4311-ade3-c11b766b69ab", "email": "1234@1234", "owner_id": "35c253c0-...", "status": "active" }
]
```

### 9.6.2 E2 員工透過 RLS 直接 SELECT markets 表（**攻擊面 #4 確認**）

> 使用 `staff_id = 5e92b457-1eaf-49eb-9295-ba47b5a3e575`

```sql
SET LOCAL ROLE authenticated;
SET LOCAL request.jwt.claim.sub TO '5e92b457-1eaf-49eb-9295-ba47b5a3e575';

SELECT id, name, booth_cost, commission_rate, total_profit
FROM markets
WHERE id IN (SELECT market_id FROM market_members WHERE user_id = '5e92b457-1eaf-49eb-9295-ba47b5a3e575')
LIMIT 1;
```

**線上實測輸出**：
```json
[
  {
    "id": "edb3c4cb-0b8d-4bfa-a42f-8a961c4abd9e",
    "name": "2222",
    "booth_cost": "0.00",        // ❌ 員工透過 RLS 直接讀到
    "commission_rate": null,      // ❌ 員工透過 RLS 直接讀到
    "total_profit": "0.00"        // ❌ 員工透過 RLS 直接讀到
  }
]
```

**結論**：🟠 P0 **攻擊面 #4 100% 確認**。員工用 DevTools 跑 `supabase.from('markets').select('*')` 透過 RLS 即可拉到 `booth_cost` / `commission_rate` / `total_profit`。

### 9.6.3 E3 員工透過 view 讀 events.payload（**攻擊面 #3 確認**）

> 使用 `staff_id = 5e92b457-1eaf-49eb-9295-ba47b5a3e575`

```sql
SET LOCAL ROLE authenticated;
SET LOCAL request.jwt.claim.sub TO '5e92b457-1eaf-49eb-9295-ba47b5a3e575';

SELECT
  type,
  payload->>'boothCost' AS booth_cost,
  payload->>'cost' AS cost,
  payload->>'supplierInfo' AS supplier,
  payload->>'profitMargin' AS profit_margin,
  payload->>'deposit' AS deposit
FROM staff_accessible_events
WHERE market_id IN (SELECT market_id FROM market_members WHERE user_id = '5e92b457-1eaf-49eb-9295-ba47b5a3e575')
AND type IN ('market_created', 'product_created', 'deal_closed', 'cost_added')
ORDER BY timestamp DESC LIMIT 10;
```

**線上實測輸出**（10 筆 `market_created`）：
```json
[
  { "type": "market_created", "booth_cost": "3000", "deposit": "0" },
  { "type": "market_created", "booth_cost": "3000", "deposit": "0" },
  { "type": "market_created", "booth_cost": "0", "deposit": "0" },
  { "type": "market_created", "booth_cost": "0", "deposit": "0" },
  { "type": "market_created", "booth_cost": "0", "deposit": "0" },
  { "type": "market_created", "booth_cost": "0", "deposit": "0" },
  { "type": "market_created", "booth_cost": "3500", "deposit": "0" },
  { "type": "market_created", "booth_cost": "3500", "deposit": "0" },
  { "type": "market_created", "booth_cost": "3500", "deposit": "0" },
  { "type": "market_created", "booth_cost": "3500", "deposit": "0" }
]
```

**結論**：🟠 P0 **攻擊面 #3 100% 確認**。員工用 DevTools 跑 `supabase.from('staff_accessible_events').select('*')` 即可拉到 `payload.boothCost` 真實金額（3000, 3500 等），還有 `deposit`。

`cost` / `supplier` / `profit_margin` 是 null 是因為這個 owner 還沒建 `product_created` / `deal_closed` 事件，**不代表 view 結構過濾了**——view 結構是**完全暴露**的（員工 DevTools 看 `select * from staff_accessible_events` 就能看到 JSONB 全部內容）。

### 9.6.4 攻擊面彙整

| 攻擊面 | 線上實測 | 證據 |
|---|---|---|
| #1 `staff_accessible_markets` view | ✅ **真實可重現** | D1 確認 8 個敏感欄位在 view schema 中 |
| #2 `staff_accessible_products` view | ✅ **真實可重現** | D2 確認 `cost` 欄位在 view schema 中 |
| #3 `staff_accessible_events` view | ✅ **真實可重現** | **E3 實測拉到 10 筆真實 `boothCost=3000/3500`** |
| #4 RLS 允許員工直接 SELECT 底表 | ✅ **真實可重現** | **E2 實測拉到真實 `booth_cost=0.00` / `total_profit=0.00`** |

**最終判定**：🟠 P0 攻擊面**全部真實可重現**，**必須**在 C2.29B 修復。

---

## 9.7 C2.29B 草稿 A 線上對齊版（基於實測）

> 草稿 A 微調：
> - 員工路徑的 SELECT 列表中需保留 `is_deleted` / `sync_status` / `operation_phase` / `is_collaborative`（線上 view 都有）
> - events view 需保留 `e.sync_status` 與 `e.created_at` 欄位（線上比 `001` 多 2 個）

完整草稿 SQL 將在 C2.29B 啟動時提供獨立 migration file（`supabase/migrations/039_staff_view_hardening_v2.sql`），**不直接套用**。

---

## 9.7 C2.29B-1 啟動紀錄（2026-06-15）

> **狀態**：🟡 草稿已建立，**已 commit 到 repo，**未套用到 Supabase**\*\*。

### 9.7.1 新增檔案

| 檔案 | 用途 |
|---|---|
| `supabase/migrations/039_staff_view_hardening.sql` | C2.29B-1 view hardening 草稿（Staff branch 脫敏 + helper function） |
| `docs/C2.29_VIEW_BACKUP_2026_06_15.md` | 線上 view 定義備份 + rollback 對照 |

### 9.7.2 039 草稿設計摘要

| View | Staff branch 處理 | Owner branch 處理 |
|---|---|---|
| `staff_accessible_markets` | 8 個敏感欄位 → NULL（booth_cost / total_profit / commission_rate / 4 rental / registration_fee）；deposit / 設備計數 / 基本資訊保留 | 全部保留 |
| `staff_accessible_products` | `cost` → NULL；price / stock / name / 其他保留 | 全部保留 |
| `staff_accessible_events` | 4 branch 都呼叫 `sanitize_staff_event_payload()` 處理 payload；tombstone（deal_deleted / interaction_deleted）保留 | 全部保留 |

### 9.7.3 sanitize_staff_event_payload 處理策略

- **Top-level 移除** 17 個敏感 key（camelCase + snake_case）：
  - 攤位費用：`boothCost` / `booth_cost` / `registrationFee` / `registration_fee` / `commissionRate` / `commission_rate` / `tableRental` / `table_rental` / `chairRental` / `chair_rental` / `umbrellaRental` / `umbrella_rental` / `tableclothRental` / `tablecloth_rental`
  - 商品成本：`cost` / `costAtTimeOfSale` / `cost_at_time_of_sale` / `supplierInfo` / `supplier_info` / `totalCost` / `total_cost`
  - 利潤：`profitMargin` / `profit_margin` / `grossMargin` / `gross_margin` / `totalProfit` / `total_profit` / `netProfit` / `net_profit` / `profit`
- **`items[]` 陣列遞迴處理**：對 `deal_closed.payload.items[]` 內的每個 object 套用同樣的 key 移除
- **tombstone 保留**：`deal_deleted` / `interaction_deleted` 不在移除清單
- **安全標記**：`SECURITY INVOKER` / `IMMUTABLE` / `SECURITY INVOKER`

### 9.7.4 套用前必讀

1. 必須先建立 `docs/C2.29_VIEW_BACKUP_2026_06_15.md`（**已完成**）
2. 必須在 Supabase SQL Editor 套用本檔
3. 套用後執行 §9.7.5 驗證 SQL
4. 驗證通過後再 commit 套用紀錄

### 9.7.5 套用後驗證 SQL（人工執行）

驗證 SQL 已包含在 `039_staff_view_hardening.sql` Section 5：

1. **驗證 1**：Staff 查 `staff_accessible_markets` 敏感欄位應為 NULL（booth_cost / total_profit / commission_rate / 4 rental / registration_fee）
2. **驗證 2**：Staff 查 `staff_accessible_products` cost 應為 NULL
3. **驗證 3**：Staff 查 `staff_accessible_events.payload` 敏感 key 應被移除（含 `items[]` 巢狀）
4. **驗證 4**：Staff 仍應看到 `deal_deleted` / `interaction_deleted` tombstone
5. **驗證 5**：Owner 查詢仍保留完整資料

### 9.7.6 範圍限制遵守

- ✅ 039 只能修 staff_accessible_* view 層
- ✅ 039 不修改底表 RLS（**攻擊面 #4 留給 C2.29B-2**）
- ✅ 039 不修改既有 migration
- ✅ 039 不修改前端 / PermissionGate / useUserRole
- ✅ 039 不直接套用到 Supabase

---

## C2.29B-1 Apply Result（2026-06-15）

> **狀態**：🟢 **已套用 Supabase，驗證通過**。
> 詳見 `docs/C2.29B-1_APPLICATION_CHECKLIST.md`（套用步驟）+ `docs/C2.29_VIEW_BACKUP_2026_06_15.md`（rollback 依據）。

### 套用基本資料

| 項目 | 值 |
|---|---|
| 套用對象 | `supabase/migrations/039_staff_view_hardening.sql` |
| 套用 commit | `439f97f`（含 `NULL::numeric(10,2)` / `numeric(5,2)` 精確 typmod 修正） |
| 套用方式 | Supabase SQL Editor，**整段包進 transaction**（`BEGIN;` ... `COMMIT;`） |
| 套用時間 | 2026-06-15 |
| 套用者 | 用戶（手動） |
| 套用結果 | ✅ 5 個驗證 block 全部通過 |

### 套用過程遇到的小問題（已修正）

- **問題**：dry run 時 `039_staff_view_hardening.sql` 草稿用 `NULL::numeric` 通用型別，與線上 `staff_accessible_markets` / `staff_accessible_products` 的精確 typmod（`numeric(10,2)` / `numeric(5,2)`）不完全對齊。
- **修正**：把 Staff branch 9 個 NULL cast 改成精確 typmod（commit `439f97f`）。
  - 7 個 `numeric(10,2)`：`registration_fee` / `booth_cost` / `table_rental` / `chair_rental` / `umbrella_rental` / `tablecloth_rental` / `total_profit`（markets view）
  - 1 個 `numeric(5,2)`：`commission_rate`（markets view）
  - 1 個 `numeric(10,2)`：`cost`（products view）

### Staff 驗證（攻擊面 #1-#3 全部消除）

- ✅ `staff_accessible_markets`：
  - `booth_cost` / `commission_rate` / `total_profit` / `table_rental` / `chair_rental` / `umbrella_rental` / `tablecloth_rental` / `registration_fee` 全部為 NULL
  - `deposit` / `total_revenue` / `total_deals` / `total_interactions` 保留（合理）
- ✅ `staff_accessible_products`：
  - `cost` 為 NULL
  - `price` / `stock` / `name` 等保留
- ✅ `staff_accessible_events.payload`：
  - top-level 敏感 key 全部不存在：`boothCost` / `cost` / `costAtTimeOfSale` / `supplierInfo` / `profitMargin` / `grossMargin` / `totalProfit` / `booth_cost` / `commissionRate` 全部 `has_* = false`
  - `deal_closed.payload.items[]` 巢狀結構敏感 key 也已脫敏
- ✅ Tombstone 仍可見：`deal_deleted` / `interaction_deleted` 員工仍可查（攻擊面修復**不**犧牲 tombstone 顯示）

### Owner 驗證（無 regression）

- ✅ Owner 透過 `staff_accessible_markets` 仍可看到完整 `booth_cost` / `total_profit` / `commission_rate` 等 financial fields
- ✅ Owner 透過 `staff_accessible_products` 仍可看到完整 `cost` / `price`
- ✅ Owner 透過 `staff_accessible_events` 仍可看到完整 `payload`（含 `boothCost` / `cost` / `supplierInfo` 等所有 key）

### 套用前 commit 紀錄

- `98fb4ea` db(c2): draft hardened staff accessible views
- `d5c43c2` docs(c2): add C2.29 live verification (E1-E3 attack repro)
- `e85209a` docs(db): clarify transactional verification for staff view hardening
- `a9d5d77` docs(c2): add C2.29B-1 application checklist
- `439f97f` fix(db): preserve numeric typmods in staff view hardening draft
- **套用日 commit hash**：`439f97f`（最終套用版）

### ⚠️ C2.29B-1 的範圍限制（重要提醒）

- ✅ **C2.29B-1 只修 staff_accessible_* view 層**
- ⚠️ **C2.29B-1 沒修底表 RLS**：E2 已證明 Staff 仍可能透過底表 RLS 直接 SELECT `markets` 取得 `booth_cost` / `commission_rate` / `total_profit`（攻擊面 #4 仍然存在）。
- ⚠️ **C2.29B-1 沒改前端查詢路徑**：前端 `lib/supabase/markets.ts` / `products.ts` / events 相關查詢仍是直接打底表，員工 DevTools 仍可繞過 view 攻擊底表。
- 🟡 **C2.29B-2 仍需處理**：
  1. 底表 RLS 收緊（草稿 B 見 §7.2）
  2. 前端查詢路徑收斂（改走 `staff_accessible_*` view 或 owner-only view）
  3. 或全面改走 SECURITY DEFINER RPC（草稿 B 候選方案 C，風險最低）

> 換言之：**C2.29B-1 已消除 3 個攻擊面（view 層洩漏），但攻擊面 #4（底表 RLS 直接查詢）仍在**。要等 C2.29B-2 才能完全關閉。

---

## 9.8 C2.29B-2 待辦（底表 RLS 收緊，**未實作**）

> **重要**：039_staff_view_hardening.sql **只能修 staff_accessible_* view 層**。
> E2 已證明 Staff 仍可能透過底表 RLS 直接 SELECT markets 取得敏感欄位。
> 因此 C2.29B-2 必須規劃 base table RLS tightening **或** 前端完全遷移到 staff-safe view / RPC 後再收緊底表。

### 9.8.1 必要前置審計

1. **Audit 所有 `supabase.from('markets')` 直接查詢**
   - 找出所有 owner 端不經 view 的直接查詢
   - 確認是否需要 owner-only RPC
2. **Audit 所有 `supabase.from('products')` 直接查詢**
   - 同上
3. **Audit 所有 `supabase.from('events')` 直接查詢**
   - 同上
4. **確認前端都改走 safe view / RPC**
   - 全部 owner 端查詢也改走 view（或新增 owner-only view）
   - 或改走 SECURITY DEFINER RPC

### 9.8.2 C2.29B-2 候選方案

| 方案 | 描述 | 風險 | 優先度 |
|---|---|---|---|
| **A. 收緊底表 RLS** | 把底表 RLS 改為「僅 owner 透過 view / RPC 可讀敏感欄位」 | 🟠 高：可能破壞現有 trigger / 同步邏輯 | 🟡 P1 |
| **B. 收緊 + 新增 owner-only view** | 保留底表 owner RLS，新增 owner-only view | 🟡 中：需 owner 端全部遷移 | 🟡 P1 |
| **C. 完全遷移到 RPC** | 全部查詢改走 SECURITY DEFINER RPC | 🟢 低：可控性最高 | 🟡 P1 |
| **D. 不動底表 RLS，僅補前端脫敏** | 攻擊面 #4 留作已知風險 | 🟠 高：員工 DevTools 仍可攻擊 | 🟠 P0 不可接受 |

### 9.8.3 C2.29B-2 啟動條件檢查

- [ ] 039 已套用到 Supabase
- [ ] 039 套用後驗證 SQL 全部通過
- [ ] 前端審計完成（4.1-4.4）
- [ ] 確認 owner 端改走 view / RPC 計畫
- [ ] 用戶明確授權 C2.29B-2 啟動

### 9.8.4 範圍限制

- ❌ 不要在 039 草稿內實作 RLS 收緊
- ❌ 不要在 C2.29B-1 階段動 C2.29B-2 計畫
- ✅ 只能建立 039 草稿與必要文件更新

---

## 10. 範圍限制遵守確認

- ✅ 只做只讀分析（不套 SQL）
- ✅ 不直接 CREATE OR REPLACE VIEW
- ✅ 不直接修改 Supabase migration（既有檔不動）
- ✅ 不動前端 PermissionGate
- ✅ 不動 useUserRole / C2.28 已完成邏輯
- ✅ 不動 C2.30C/D / C2.31
- ✅ 不改底表 RLS（C2.29B-2 處理）

---

## 11. 參考文件

- `docs/DATA_CONVERGENCE_PLAN.md` — 主收斂計畫
- `docs/OWNER_STAFF_REVENUE_HARDENING_PLAN.md` — C2.23-C2.31 員工權限加固原始計畫
- `docs/CONVERGENCE_ARCHIVE.md` — 收斂文件總檔
- `docs/C2.27_REANALYSIS_2026_06_15.md` — C2.27 收尾報告
- `docs/C2.28_REANALYSIS_2026_06_15.md` — C2.28 重新分析報告
- `lib/permissions/PermissionGate.ts` — 統一脫敏閘道（C2.30C）
- `lib/permissions/role-fail-closed.ts` — C2.28 fail-closed 純函式
- `hooks/useUserRole.ts` — C2.28 fail-closed hook
- `lib/sync-context.tsx` — C2.28 sync-context fail-closed
- `supabase/migrations/038_ensure_staff_tombstone_events_visible.sql` — 最新 staff_accessible_events
- `supabase/migrations/030_fix_data_isolation.sql` — view 重建
- `supabase/migrations/035_fix_p0_rls_security.sql` — P0 RLS 加固
- `supabase/migrations/20240220_add_staff_roles.sql` — 早期過濾版 view（已廢棄）
- `lib/supabase/markets.ts` — 前端 markets 查詢（用 view）
- `lib/supabase/products.ts` — 前端 products 查詢（用 view）
