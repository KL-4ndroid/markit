# C2.29B Staff View / RLS Read-Only Verification Result

Date: 2026-06-29
Environment: production Supabase
Runner: user
Supabase project: not recorded in this document
Owner id: `0d21abfe-136f-4c42-987b-14928593f323`
Staff id: `5e92b457-1eaf-49eb-9295-ba47b5a3e575`

SQL source:

- `supabase/verification/c2_29b_staff_view_rls_read_only.sql`

Safety confirmation:

- [x] Only read-only SQL output was provided.
- [ ] Full raw output for every SELECT was preserved.
- [ ] No migration was executed as part of this C2.29B verification record.
- [ ] No RLS/policy/function/schema change was executed as part of this C2.29B verification record.
- [ ] No `INSERT`, `UPDATE`, `DELETE`, `UPSERT`, or data repair was executed as part of this C2.29B verification record.

Note:

```text
Supabase SQL Editor appears to have returned only the last SELECT result for several multi-SELECT blocks.
This document records the provided output as partial evidence and identifies the missing follow-up queries.
Use supabase/verification/c2_29b_staff_view_rls_read_only_followup.sql to collect the missing evidence as one result set per block.
```

## C2.29B-E0 Candidate Active Owner/Staff Pairs

Result summary:

```text
Row count: 2
Chosen owner_id: 0d21abfe-136f-4c42-987b-14928593f323
Chosen staff_id: 5e92b457-1eaf-49eb-9295-ba47b5a3e575
Chosen related_market_count: 30
Chosen staff role: operator

Additional active pair:
- owner_id 35c253c0-540f-436f-abb5-c1b116e1f3df
- staff_id 85d316ce-5871-4311-ade3-c11b766b69ab
- related_market_count 6
- role operator
```

Assessment:

- [x] Pass: usable active owner/staff pair exists.
- [ ] Blocked: no active staff relationship available.
- [ ] Blocked/error:

Notes:

```text
Use the first pair for follow-up verification because it has the broadest staff-accessible market coverage.
```

## C2.29B-E1 Staff Market View Redaction And Scope

Provided output:

```text
deleted_market_rows: 0
```

Assessment:

- [ ] Pass: staff sees only staff rows, sensitive fields are null, and no duplicate/deleted rows appear.
- [ ] Pass with notes: zero staff-visible market rows are expected for this account.
- [x] Partial: deleted market filtering passed, but redaction/scope/duplicate outputs were not returned.
- [ ] Needs review: one or more sensitive fields are exposed, owner rows appear, duplicates appear, or deleted rows appear.
- [ ] Blocked/error:

Missing evidence:

```text
access_type counts
total_markets
booth_cost_null
total_profit_null
commission_rate_null
registration_fee_null
deposit_null
duplicate_market_id_rows
```

## C2.29B-E2 Staff Product View Redaction

Result summary:

```text
total_products: 2
cost_null: 2
```

Assessment:

- [x] Pass: product `cost` is redacted for all returned staff-visible products.
- [ ] Pass with notes: zero staff-visible products are expected for this account.
- [ ] Needs review: product cost is exposed.
- [ ] Blocked/error:

Notes:

```text
This section passed with the provided output.
```

## C2.29B-E3 Staff Event Payload Redaction, Tombstones, And Duplicate Ids

Provided output:

```text
deal_deleted: 35
interaction_deleted: 1
```

Assessment:

- [ ] Pass: staff sees only staff rows, all sensitive payload key counts are zero, and duplicate event ids are zero.
- [ ] Pass with notes: zero staff-visible events are expected for this account.
- [x] Partial: tombstone visibility passed, but payload redaction/scope/duplicate outputs were not returned.
- [ ] Needs review: sensitive payload keys, owner rows, or duplicate event ids appear.
- [ ] Blocked/error:

Missing evidence:

```text
access_type counts
total_events
all has_* sensitive payload key counts
duplicate_event_id_rows
```

## C2.29B-E4 Staff Direct Base-Table SELECT Blocked

Provided output:

```text
staff_events_with_payload: 0
```

Assessment:

- [ ] Pass: all staff direct base-table counts are zero.
- [x] Partial: direct events payload leakage check returned zero, but the main direct base-table counts were not returned.
- [ ] Needs review: staff can read direct base-table rows.
- [ ] Blocked/error:

Missing evidence:

```text
staff_markets_direct
staff_products_direct
staff_events_direct
staff_markets_with_booth_cost
staff_products_with_cost
```

## C2.29B-E4 Owner Direct Base-Table Regression Check

Provided output:

```text
owner sample rows returned: 10
financial fields visible to owner: yes
```

Assessment:

- [ ] Pass: owner can still read expected owner-owned base-table rows and financial fields.
- [x] Partial: owner financial sample rows are visible, but direct base-table counts were not returned.
- [ ] Pass with notes: chosen owner has limited/no data but no permission regression is shown.
- [ ] Needs review: owner reads are blocked or owner financial fields are unexpectedly hidden.
- [ ] Blocked/error:

Notes:

```text
The provided sample confirms owner financial fields are not globally forced null.
The follow-up SQL should still capture owner_markets_direct, owner_products_direct, and owner_events_direct counts.
```

## C2.29B-E5 Type-Level Guard

Command:

```powershell
npm.cmd run test:staff-types
```

Result summary:

```text
Not part of the provided SQL output.
Previously passed locally after adding tsconfig.staff-typed-client.json.
```

Assessment:

- [x] Pass: type-level guard compiles with expected forbidden-base-table assertions.
- [ ] Needs review: type-level guard is missing, stale, or failing.
- [ ] Blocked/error:

## Overall C2.29B Decision

Overall result:

- [ ] Pass: staff view/RLS verification does not block the next plan.
- [ ] Pass with notes: findings exist but are understood and not blocking.
- [x] Needs follow-up read-only output before final decision.
- [ ] Requires explicit RLS/view/client decision before continuing.

Recommended next step:

```text
Run supabase/verification/c2_29b_staff_view_rls_read_only_followup.sql with:
- STAFF_USER_UUID = 5e92b457-1eaf-49eb-9295-ba47b5a3e575
- OWNER_USER_UUID = 0d21abfe-136f-4c42-987b-14928593f323

This should collect the missing E1/E3/E4/owner count evidence as one result set per block.
No repair or migration is currently justified by the partial output.
```
