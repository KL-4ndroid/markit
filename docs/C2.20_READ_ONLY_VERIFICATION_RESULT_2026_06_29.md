# C2.20 Staff Data Flow Read-Only Verification Result

Date: 2026-06-29
Environment: user-provided Supabase SQL Editor output
Runner: user
Supabase project: not recorded in output
Owner id: `0d21abfe-136f-4c42-987b-14928593f323`
Staff id: `5e92b457-1eaf-49eb-9295-ba47b5a3e575`
Staff role: `operator`

SQL source:

- `supabase/verification/c2_20_staff_data_flow_read_only.sql`

Safety confirmation:

- [x] Only read-only SQL was reported.
- [x] No migration was reported.
- [x] No RLS/policy/function/schema change was reported.
- [x] No `INSERT`, `UPDATE`, `DELETE`, `UPSERT`, or data repair was reported.

## C2.20-0 Candidate Active Owner/Staff Pairs

Result summary:

```text
Row count: 2
Chosen owner_id: 0d21abfe-136f-4c42-987b-14928593f323
Chosen staff_id: 5e92b457-1eaf-49eb-9295-ba47b5a3e575
Chosen related_market_count: 46
Chosen staff role: operator
```

Assessment:

- [x] Pass: usable active owner/staff pair exists.
- [ ] Blocked: no active staff relationship available.
- [ ] Blocked/error:

Notes:

```text
The first active owner/staff pair has the broadest market coverage and is suitable for C2.20 verification.
```

## C2.20-A Staff View Definitions

Result summary:

```text
staff_accessible_markets definition returned: not provided in output
staff_accessible_products definition returned: not provided in output
staff_accessible_events definition returned: not provided in output
sanitize_staff_event_payload function returned: yes
```

Assessment:

- [ ] Pass: view/function definitions are present and consistent with staff data flow expectations.
- [x] Needs review: function exists, but view definitions were not included in the pasted output.
- [ ] Blocked/error:

Notes:

```text
sanitize_staff_event_payload exists and removes many sensitive keys recursively, including cost, supplier, profit, rental, commission, and registration keys.
The function definition does not remove top-level `deposit`.
This matches the C2.20-G finding where staff-visible event payloads still contain `deposit`.
```

Follow-up:

```text
Run C2.20-A1 because Supabase SQL Editor appears to have returned only the final function result set from C2.20-A.
```

## C2.20-B Tombstone Root Market Id Coverage

Result summary:

```text
deal_deleted tombstone_count: 37
deal_deleted missing_root_market_id: 0
deal_deleted missing_target_event_id: 0
interaction_deleted tombstone_count: 1
interaction_deleted missing_root_market_id: 0
interaction_deleted missing_target_event_id: 0
```

Assessment:

- [x] Pass: all missing counts are zero.
- [ ] Needs review: missing root market id or target event id exists.
- [ ] Blocked/error:

Notes:

```text
Global tombstone structure is healthy.
```

## C2.20-C Tombstones Missing Root Market Id

Result summary:

```text
Row count: 0
Notable event ids: none
```

Assessment:

- [x] Pass: zero rows.
- [ ] Needs review: missing-root tombstones exist.
- [ ] Blocked/error:

Notes:

```text
No missing-root tombstone rows were reported.
```

## C2.20-D Explicit Owner/Staff Relationship Event Coverage

Result summary:

```text
deal_closed event_count: 231
deal_deleted event_count: 35
interaction_recorded event_count: 97
interaction_deleted event_count: 1
missing_root_market_id values: all 0
```

Assessment:

- [x] Pass: relationship-scoped event coverage is present and internally consistent.
- [ ] Pass with notes: selected staff has limited/no tombstone coverage but no contradiction was found.
- [ ] Needs review: relationship coverage contradicts staff view output.
- [ ] Blocked/error:

Notes:

```text
The selected active owner/staff relationship has enough events and tombstones for meaningful C2.20 verification.
```

## C2.20-E Staff-Session View Counts

Result summary:

```text
staff_markets_view: not provided in output
staff_products_view: not provided in output
staff_events_view: not provided in output
event type counts:
- deal_closed: 234
- deal_deleted: 36
- interaction_deleted: 2
- interaction_recorded: 44
```

Assessment:

- [ ] Pass: staff views return expected scoped rows.
- [x] Pass with notes: staff event view returns scoped projection/tombstone events, but aggregate market/product/event counts were not included in pasted output.
- [ ] Needs review: counts are unexpectedly zero or unexpectedly broad.
- [ ] Blocked/error:

Notes:

```text
Staff-visible event counts are close but not identical to the explicit relationship query.
That may be valid because the staff_accessible_events view can include additional owner/global branches depending on deployed view definition.
The missing view definition output from C2.20-A should be reviewed before treating this difference as a defect.
```

## C2.20-F Staff-Session Tombstone Visibility

Result summary:

```text
Initial result: query error because created_at was not exposed by the view.
Corrected rerun sample row count: 20
Sample event types:
- deal_deleted
- interaction_deleted
Sample rows preserve market_id: yes
Sample rows preserve target event id: yes, via payload_event_id_camel
Duplicate ids observed in sample:
- 60fe11fd-de99-41fb-9be5-ee0503dab15b appeared twice
- a0250b35-49b7-40c9-9e92-629b62aa3674 appeared twice
```

Assessment:

- [ ] Pass: staff-visible tombstones are present when expected and preserve target ids.
- [ ] Pass with notes: no tombstones exist for chosen staff scope.
- [x] Needs review: corrected query shows tombstones, but duplicate event ids appear in the staff view sample.
- [ ] Blocked/error:

Notes:

```text
The corrected C2.20-F confirms staff-visible tombstones are available and keep target event ids.
However, duplicate ids in the sample suggest staff_accessible_events may have overlapping view branches or another duplicate source.
Run C2.20-J to quantify duplicate event ids before changing any replay or view behavior.
```

## C2.20-G Staff-Session Event Payload Sensitive-Key Check

Result summary:

```text
total_events: 436
has_boothCost: 0
has_cost: 0
has_costAtTimeOfSale: 0
has_supplierInfo: 0
has_profitMargin: 0
has_grossMargin: 0
has_totalProfit: 0
has_netProfit: 0
has_deposit: 30
has_commissionRate: 0
has_tableRental: 0
has_chairRental: 0
has_umbrellaRental: 0
has_tableclothRental: 0
has_registrationFee: 0
has_booth_cost: 0
has_total_cost: 0
has_supplier_info: 0
```

Assessment:

- [ ] Pass: all sensitive key counts are zero.
- [x] Needs review: `deposit` appears in staff-visible payloads.
- [ ] Blocked/error:

Notes:

```text
All checked sensitive event payload keys are absent except `deposit`, which appears in 30 staff-visible event payloads.
The deployed sanitize_staff_event_payload function does not remove `deposit`.
Before any RLS/view/sanitizer change, classify what `deposit` means in these payloads:
- If it is an owner-only financial field, this is a sanitizer gap.
- If it is a non-sensitive operational field, the C2.20 checklist should be updated to stop treating it as a leak.
No automatic repair or policy change is approved by this finding.
```

## C2.20-H Staff-Session Market/Product Sensitive-Column Check

Result summary:

```text
total_markets: not provided in output
booth_cost_null: not provided in output
total_profit_null: not provided in output
commission_rate_null: not provided in output
registration_fee_null: not provided in output
total_products: 2
cost_null: 2
```

Assessment:

- [ ] Pass: sensitive columns are null for staff-visible market/product rows.
- [x] Pass with notes: product cost is redacted, but market sensitive-column output was not included.
- [ ] Needs review: staff-visible rows expose sensitive columns.
- [ ] Blocked/error:

Notes:

```text
Product cost redaction passed for the selected staff session.
Market sensitive-column redaction still needs the first C2.20-H result set.
```

Follow-up:

```text
Run C2.20-H1 because Supabase SQL Editor appears to have returned only the final product result set from C2.20-H.
```

## C2.20-I Staff-Session Deposit Payload Classification Sample

Result summary:

```text
Row count: 30
Event types: market_created only
Deposit values observed: mostly 0, with non-zero examples including 1000
Notable non-zero examples:
- 196b0b2e-7f0a-4e7c-ad04-b1e5e3572771: market_created, deposit 1000
- 632ecda6-c623-4ab8-b514-7ac151290dc7: market_created, deposit 1000
- 836df194-b70e-46c2-8de0-2bcd2fe8a81f: market_created, deposit 1000
```

Assessment:

- [ ] Pass: `deposit` is confirmed non-sensitive and the checklist should be updated.
- [x] Needs review: `deposit` appears to be owner-only financial data.
- [ ] Blocked/error:

Notes:

```text
The sample confirms `deposit` appears in staff-visible market_created event payloads.
Local PermissionGate and data-sanitization tests treat market/event `deposit` as sensitive for staff.
Therefore the deployed sanitize_staff_event_payload function appears inconsistent with local sanitizer rules.
This is a likely cloud view/function sanitizer gap, but no RLS/view/function change is approved yet.
```

## C2.20-J Staff-Session Duplicate Event Id Check

Result summary:

```text
Not run yet.
```

Assessment:

- [ ] Pass: zero duplicate event id rows.
- [x] Needs review: C2.20-F sample suggests duplicate event ids; C2.20-J should quantify.
- [ ] Blocked/error:

Notes:

```text
Run C2.20-J before final C2.20 classification.
```

## Overall C2.20 Decision

Overall result:

- [ ] Pass: staff data flow verification does not block C2.29B.
- [ ] Pass with notes: findings exist but are understood and not blocking C2.29B.
- [ ] Needs follow-up analysis before C2.29B.
- [x] Requires explicit RLS/view/sanitizer decision before continuing.

Recommended next step:

```text
Do not proceed to C2.29B final verification yet.
First complete the low-risk C2.20 follow-up:
1. Run C2.20-A1 for standalone staff view definitions.
2. Run C2.20-H1 for standalone market sensitive-column output.
3. Run C2.20-J to quantify duplicate event ids in staff_accessible_events.
4. Decide whether to draft a sanitizer/RLS/view migration to remove `deposit` from staff-visible event payloads.

No code, RLS, view, sanitizer, or data repair decision is approved yet.
```
