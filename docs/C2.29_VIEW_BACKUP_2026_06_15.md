# C2.29 View Backup / Rollback 文件

> **建立日期**：2026-06-15
> **建立者**：Cursor (Codex)
> **狀態**：🟡 View 定義快照（已從線上抓取 A1/A2/A3 查詢結果保存）
> **對應任務**：C2.29B-1（草稿建立階段，**未套用** 039 migration）
> **範圍限制**：本文件**僅**保存 view 定義作為 rollback 對照依據，**不直接執行任何 SQL**

---

## 0. 用途

1. 套用 `039_staff_view_hardening.sql` 前，保留目前線上 `staff_accessible_*` view 完整 SQL
2. 若 039 套用後出現問題，可依本文件 SQL **手動**還原 view
3. 用戶做 diff 對照（套用前 vs 套用後）

**⚠️ Rollback SQL 也不要自動執行，需人工確認。**

---

## 1. 目前線上 `staff_accessible_markets` 定義

> 來源：用戶在 Supabase SQL Editor 跑 A1 查詢 `pg_views` 取得
> 抓取日期：2026-06-15

```sql
SELECT
  schemaname,
  viewname,
  definition
FROM pg_views
WHERE schemaname = 'public'
  AND viewname = 'staff_accessible_markets';
```

**線上 view 定義**：

```sql
 SELECT m.id,
    m.owner_id,
    m.name,
    m.location,
    m.start_date,
    m.end_date,
    m.status,
    m.early_entry_enabled,
    m.early_entry_time,
    m.check_in_time,
    m.operating_start_time,
    m.operating_end_time,
    m.registration_fee,
    m.booth_cost,
    m.deposit,
    m.table_rental,
    m.chair_rental,
    m.umbrella_rental,
    m.tablecloth_rental,
    m.commission_rate,
    m.table_free,
    m.chair_free,
    m.umbrella_free,
    m.tablecloth_free,
    m.total_revenue,
    m.total_profit,
    m.total_interactions,
    m.total_deals,
    m.notes,
    m.created_at,
    m.updated_at,
    m.is_collaborative,
    m.operation_phase,
    m.is_deleted,
    m.sync_status,
    sr.owner_id AS relationship_owner_id,
    sr.permissions,
    'staff'::text AS access_type
   FROM ((markets m
     JOIN market_members mm ON ((mm.market_id = m.id)))
     JOIN staff_relationships sr ON ((sr.owner_id = mm.user_id)))
  WHERE ((sr.staff_id = auth.uid()) AND (sr.status = 'active'::text))
UNION ALL
 SELECT m.id,
    m.owner_id,
    m.name,
    m.location,
    m.start_date,
    m.end_date,
    m.status,
    m.early_entry_enabled,
    m.early_entry_time,
    m.check_in_time,
    m.operating_start_time,
    m.operating_end_time,
    m.registration_fee,
    m.booth_cost,
    m.deposit,
    m.table_rental,
    m.chair_rental,
    m.umbrella_rental,
    m.tablecloth_rental,
    m.commission_rate,
    m.table_free,
    m.chair_free,
    m.umbrella_free,
    m.tablecloth_free,
    m.total_revenue,
    m.total_profit,
    m.total_interactions,
    m.total_deals,
    m.notes,
    m.created_at,
    m.updated_at,
    m.is_collaborative,
    m.operation_phase,
    m.is_deleted,
    m.sync_status,
    m.owner_id AS relationship_owner_id,
    '{"can_edit": true, "can_view": true}'::jsonb AS permissions,
    'owner'::text AS access_type
   FROM (markets m
     JOIN market_members mm ON ((mm.market_id = m.id)))
  WHERE (mm.user_id = auth.uid());
```

### 1.1 Rollback SQL（**禁止自動執行**）

```sql
-- 🔴 ROLLBACK 草稿：套用 039 後若需還原，手動執行
-- ⚠️ 必須先確認 039 已被套用
-- ⚠️ 必須人工確認資料完整性
-- ⚠️ 還原後員工可重新查到全部敏感欄位（攻擊面重現）

CREATE OR REPLACE VIEW public.staff_accessible_markets AS
[貼上上述完整 SQL];
```

**保留欄位（owner + staff 兩 branch 都含 38 個欄位）**：
- 識別：`id` / `owner_id` / `relationship_owner_id` / `access_type` / `permissions`
- 基本：`name` / `location` / `start_date` / `end_date` / `status` / `notes`
- 時間：`early_entry_*` / `check_in_time` / `operating_start_time` / `operating_end_time`
- 費用（敏感）：`registration_fee` / `booth_cost` / `deposit` / `table_rental` / `chair_rental` / `umbrella_rental` / `tablecloth_rental` / `commission_rate`
- 計數：`total_revenue` / `total_profit` / `total_interactions` / `total_deals`
- 設備：`table_free` / `chair_free` / `umbrella_free` / `tablecloth_free`
- 元資料：`is_collaborative` / `operation_phase` / `is_deleted` / `sync_status` / `created_at` / `updated_at`

---

## 2. 目前線上 `staff_accessible_products` 定義

> 來源：用戶在 Supabase SQL Editor 跑 A2 查詢 `pg_views` 取得
> 抓取日期：2026-06-15

```sql
SELECT
  schemaname,
  viewname,
  definition
FROM pg_views
WHERE schemaname = 'public'
  AND viewname = 'staff_accessible_products';
```

**線上 view 定義**：

```sql
 SELECT p.id,
    p.market_id,
    p.name,
    p.category,
    p.price,
    p.cost,
    p.icon_name,
    p.color_code,
    p.stock,
    p.unlimited_stock,
    p.is_active,
    p.total_sold,
    p.description,
    p.created_at,
    p.updated_at,
    p.owner_id,
    p.is_shared,
    sr.owner_id AS relationship_owner_id,
    sr.permissions,
    'staff'::text AS access_type
   FROM (products p
     JOIN staff_relationships sr ON ((sr.owner_id = p.owner_id)))
  WHERE ((sr.staff_id = auth.uid()) AND (sr.status = 'active'::text) AND (p.is_active = true))
UNION ALL
 SELECT p.id,
    p.market_id,
    p.name,
    p.category,
    p.price,
    p.cost,
    p.icon_name,
    p.color_code,
    p.stock,
    p.unlimited_stock,
    p.is_active,
    p.total_sold,
    p.description,
    p.created_at,
    p.updated_at,
    p.owner_id,
    p.is_shared,
    p.owner_id AS relationship_owner_id,
    '{"can_edit": true, "can_view": true}'::jsonb AS permissions,
    'owner'::text AS access_type
   FROM products p
  WHERE ((p.owner_id = auth.uid()) AND (p.is_active = true));
```

### 2.1 Rollback SQL（**禁止自動執行**）

```sql
-- 🔴 ROLLBACK 草稿：套用 039 後若需還原，手動執行
-- ⚠️ 必須人工確認
-- ⚠️ 還原後員工可重新查到 `cost` 欄位

CREATE OR REPLACE VIEW public.staff_accessible_products AS
[貼上上述完整 SQL];
```

**保留欄位（owner + staff 兩 branch 都含 21 個欄位）**：
- 識別：`id` / `market_id` / `owner_id` / `relationship_owner_id` / `access_type` / `permissions`
- 基本：`name` / `category` / `price` / `cost`（敏感）/ `description`
- 庫存：`stock` / `unlimited_stock` / `total_sold` / `is_active`
- 共享：`is_shared`
- UI：`icon_name` / `color_code`
- 元資料：`created_at` / `updated_at`

---

## 3. 目前線上 `staff_accessible_events` 定義

> 來源：用戶在 Supabase SQL Editor 跑 A3 查詢 `pg_views` 取得
> 抓取日期：2026-06-15

```sql
SELECT
  schemaname,
  viewname,
  definition
FROM pg_views
WHERE schemaname = 'public'
  AND viewname = 'staff_accessible_events';
```

**線上 view 定義**（4 個 UNION branch）：

```sql
-- Branch 1: STAFF（市集事件）
 SELECT e.id,
    e.type,
    e.payload,
    e.actor_id,
    e.market_id,
    e."timestamp",
    e.metadata,
    e.sync_status,
    sr.owner_id AS relationship_owner_id,
    sr.permissions,
    'staff'::text AS access_type
   FROM ((events e
     JOIN market_members mm ON ((mm.market_id = e.market_id)))
     JOIN staff_relationships sr ON ((sr.owner_id = mm.user_id)))
  WHERE ((sr.staff_id = auth.uid()) AND (sr.status = 'active'::text))
UNION ALL
-- Branch 2: STAFF（全局事件 market_id IS NULL）
 SELECT e.id,
    e.type,
    e.payload,
    e.actor_id,
    e.market_id,
    e."timestamp",
    e.metadata,
    e.sync_status,
    sr.owner_id AS relationship_owner_id,
    sr.permissions,
    'staff'::text AS access_type
   FROM (events e
     JOIN staff_relationships sr ON ((sr.owner_id = e.actor_id)))
  WHERE ((sr.staff_id = auth.uid()) AND (sr.status = 'active'::text) AND (e.market_id IS NULL))
UNION ALL
-- Branch 3: OWNER（自己的所有事件）
 SELECT e.id,
    e.type,
    e.payload,
    e.actor_id,
    e.market_id,
    e."timestamp",
    e.metadata,
    e.sync_status,
    e.actor_id AS relationship_owner_id,
    '{"can_edit": true, "can_view": true}'::jsonb AS permissions,
    'owner'::text AS access_type
   FROM events e
  WHERE (e.actor_id = auth.uid())
UNION ALL
-- Branch 4: OWNER（市集成員事件）
 SELECT e.id,
    e.type,
    e.payload,
    e.actor_id,
    e.market_id,
    e."timestamp",
    e.metadata,
    e.sync_status,
    mm.user_id AS relationship_owner_id,
    '{"can_edit": true, "can_view": true}'::jsonb AS permissions,
    'owner'::text AS access_type
   FROM (events e
     JOIN market_members mm ON ((mm.market_id = e.market_id)))
  WHERE ((mm.user_id = auth.uid()) AND (e.actor_id <> auth.uid()));
```

### 3.1 Rollback SQL（**禁止自動執行**）

```sql
-- 🔴 ROLLBACK 草稿：套用 039 後若需還原，手動執行
-- ⚠️ 必須人工確認
-- ⚠️ 還原後員工可重新查到完整 payload（攻擊面重現）

CREATE OR REPLACE VIEW public.staff_accessible_events AS
[貼上上述完整 4-branch SQL];
```

**保留欄位（4 個 branch 都含 11 個欄位）**：
- 識別：`id` / `type` / `actor_id` / `market_id` / `relationship_owner_id` / `access_type` / `permissions`
- 內容：`payload`（敏感：含 cost / profit / supplier / boothCost 等）/ `metadata`
- 元資料：`timestamp` / `sync_status`

---

## 4. 套用前 vs 套用後 diff 對照

| 項目 | 套用前（線上） | 套用後（039 草稿） | 差異 |
|---|---|---|---|
| `staff_accessible_markets.staff_branch.booth_cost` | 真實金額 | `NULL` | 🛡️ 脫敏 |
| `staff_accessible_markets.staff_branch.total_profit` | 真實金額 | `NULL` | 🛡️ 脫敏 |
| `staff_accessible_markets.staff_branch.commission_rate` | 真實比率 | `NULL` | 🛡️ 脫敏 |
| `staff_accessible_markets.staff_branch.*_rental` | 真實金額 | `NULL` | 🛡️ 脫敏 |
| `staff_accessible_markets.staff_branch.registration_fee` | 真實金額 | `NULL` | 🛡️ 脫敏 |
| `staff_accessible_markets.staff_branch.deposit` | 真實金額 | 保留（產品決策） | 🟢 保留 |
| `staff_accessible_markets.staff_branch.table_free/chair_free/...` | 整數 | 保留 | 🟢 保留 |
| `staff_accessible_markets.staff_branch.total_revenue/deals/interactions` | 整數 | 保留 | 🟢 保留 |
| `staff_accessible_markets.staff_branch.name/location/dates/notes` | 真實值 | 保留 | 🟢 保留 |
| `staff_accessible_markets.owner_branch.*` | 真實值 | 真實值 | ✅ 不變 |
| `staff_accessible_products.staff_branch.cost` | 真實金額 | `NULL` | 🛡️ 脫敏 |
| `staff_accessible_products.staff_branch.price/stock/name/...` | 真實值 | 保留 | 🟢 保留 |
| `staff_accessible_products.owner_branch.*` | 真實值 | 真實值 | ✅ 不變 |
| `staff_accessible_events.staff_branch.payload` | 完整 JSONB | **scrubbed**（移除 15+ 個敏感 key + 處理 nested items） | 🛡️ 脫敏 |
| `staff_accessible_events.staff_branch.deal_deleted` | 可見 | 保留 | 🟢 保留 |
| `staff_accessible_events.staff_branch.interaction_deleted` | 可見 | 保留 | 🟢 保留 |
| `staff_accessible_events.staff_branch.payload.deal_deleted` | 完整 | 保留（tombstone metadata） | 🟢 保留 |
| `staff_accessible_events.owner_branch.payload` | 完整 JSONB | 完整 JSONB | ✅ 不變 |

---

## 5. Rollback 操作流程（人工）

### 5.1 套用 039 出現問題時

1. **停止所有前端服務**（避免員工繼續查詢）
2. **評估影響範圍**（哪些 UI 異常）
3. **備份 039 套用後的 view 定義**（抓 pg_views）
4. **執行本文件 §1.1 / §2.1 / §3.1 的 rollback SQL**
5. **人工驗證**：員工重新可看到敏感欄位（攻擊面回歸已知）
6. **重啟前端服務**
7. **回填事件報告**（C2.29B-1 撤銷原因）

### 5.2 套用 039 成功時

1. **保留本文件作為歷史記錄**（不要刪除）
2. **保留 `pg_views` 套用後的定義**（新增章節到本文件）
3. **更新 §4 套用後差異表**（標記 ✅ 已套用）
4. **進入 C2.29B-2 規劃**（底表 RLS 收緊）

---

## 6. ⚠️ Rollback 限制

- Rollback **只**還原 view 結構
- Rollback **不**還原 `sanitize_staff_event_payload()` function
- Rollback **不**還原 `039_staff_view_hardening.sql` 中其他附加變更
- Rollback **不**還原 039 套用期間可能觸發的 trigger / 統計資料異動

---

## 7. 相關文件

- `docs/C2.29_REANALYSIS_2026_06_15.md` — C2.29 完整只讀分析報告
- `supabase/migrations/039_staff_view_hardening.sql` — 039 草稿（**未套用**）
- `supabase/migrations/029_fix_staff_accessible_products.sql` — 上一版 products view
- `supabase/migrations/030_fix_data_isolation.sql` — markets view 起源
- `supabase/migrations/038_ensure_staff_tombstone_events_visible.sql` — events view 起源

---

## 8. 範圍限制遵守

- ✅ 只保存 view 定義，不直接執行 SQL
- ✅ rollback SQL 標示「禁止自動執行」
- ✅ 不動既有 migration 檔
- ✅ 不動前端 / PermissionGate / useUserRole / C2.28 已完成邏輯
- ✅ 不改 RLS
