# iOS Capacitor Execution Progress

## Current Phase

Phase 2 / Backend API boundary

## Status

IN PROGRESS - Gate 1 remains passed. The Phase 2 BFF is deployed on Vercel, the server-only environment configuration and migrations 057/058/059 are reported complete, the production health/CORS/invalid-token smoke passes, the real post-cutover owner and active-staff Supabase/R2 upload paths pass, revoked-staff market access denial passes, and a live revoke-during-upload POST denial retains its local payload. The first stale-local-sync recovery reached production but exposed a canonical sale-event ID reconciliation gap; the stricter second repair is local and verified but not deployed. Gate 2 remains not passed pending that deployment/live retry, unrelated-user denial, lease/idempotency and storage-failure evidence, and remote cleanup evidence.

## Decisions

- The user authorized direct implementation on 2026-07-16, with work pausing only for material product decisions, external credentials, or manual actions.
- The user confirmed on 2026-07-17 that the production Web application is deployed on Vercel and authorized reusing that Vercel deployment for the BFF/API routes.
- The production Web/API origin is confirmed as `https://markit-app-mocha.vercel.app`.
- Web keeps same-origin `/api` calls. Static mobile builds require an explicit stable HTTPS `NEXT_PUBLIC_API_BASE_URL`.
- Authenticated CORS uses an exact allowlist and supports `capacitor://localhost`; wildcard, `null`, credentialed, malformed, and unlisted origins fail closed.
- The Vercel BFF verifies the caller with Supabase `auth.getUser(token)` and derives actor identity only from the verified user. Authorization reads remain caller-scoped under RLS; photo mutations use the separate Vercel-only server-secret RPC capability.
- Shared request policy permits one retry only for idempotent GET/HEAD requests and retryable network/status failures. Photo upload POST is never automatically retried.
- API errors expose only stable allowlisted fields. Upload/read size limits, MIME magic-byte checks, canonical R2 key binding, exact CORS checks, and fail-closed route gates are enforced server-side.
- The mobile artifact contains no API route handlers. `/api/health`, upload, and image read remain remote Vercel BFF endpoints.
- On 2026-07-19 the user reported that migrations `057_harden_sales_photo_evidence_api_boundary.sql`, `058_add_sales_photo_evidence_server_mutation_rpcs.sql`, and `059_enforce_sales_photo_evidence_server_mutation_boundary.sql` were applied to the existing owner-only Supabase environment. They must not be re-run from this workspace.
- The user approved the server-only mutation path on 2026-07-17. Token verification remains on the public client, authorization reads remain caller-scoped under RLS, and a separate Vercel-only `SUPABASE_SECRET_KEY` capability calls only the three approved photo mutation RPCs from application code.
- Migration 058 is additive: it adds the upload lease and claim/finalize/failure RPC capability while deliberately retaining the old authenticated write path for staging smoke. Migration 059 is the later permission cutover that revokes direct authenticated mutations and direct `service_role` table mutation while preserving authenticated SELECT and approved RPC execution.
- The required order is 057 hardening -> 058 additive capability -> staging RPC smoke -> 059 permission cutover -> post-cutover smoke. Migration 059 must not be applied before the staging RPC path is proven.
- On 2026-07-17 the user approved validating Phase 2 directly against the existing Vercel, Supabase, and R2 environment because the application is not externally open and is currently used only by the owner. A separate staging deployment is no longer required for this execution path.
- On 2026-07-17 the user confirmed that the Vercel Git Production Branch is `main`, matching the current local branch and `origin/main` tracking configuration.
- On 2026-07-17 the user explicitly authorized committing and pushing the complete current worktree to `origin/main` after secret scanning and verification.
- The production URL now serves the Phase 2 BFF. `APP_API_CORS_ALLOWED_ORIGINS` was corrected to the exact Web and native allowlist, then the production deployment was refreshed.
- On 2026-07-20 the user explicitly authorized temporarily removing the existing `1234@1234` operator relationship for revoked-staff testing, acknowledging that restoration may require a new invitation and acceptance. The owner removed the relationship successfully after both accounts were logged out with zero pending sync items.
- Phase 3 Capacitor/native bootstrap is not authorized by the current Gate status.

## Changed Files

- Phase 1 mobile/Web packaging: `next.config.mjs`, `package.json`, `tsconfig.mobile.json`, `.env.example`, mobile build/verification scripts, fixed detail routes, Web-only compatibility routes, shared navigation contracts, and platform/Web isolation files.
- Shared API boundary: `lib/api/client.ts`, `lib/api/contract.ts`, `lib/api/transport.ts`, `lib/api/server/auth.ts`, `lib/api/server/cors.ts`, `lib/api/server/response.ts`.
- Vercel BFF routes: `app/api/health/route.ts`, `app/api/sales-photo-evidence/upload/route.ts`, `app/api/sales-photo-evidence/image/route.ts`.
- Photo boundary hardening: `lib/sales/photo-evidence-model.ts`, `lib/sales/photo-evidence-upload-form-data.ts`, `lib/sales/photo-evidence-pending-payload-storage.ts`, both R2 server adapters, and both photo API clients.
- Revoke/rejoin retry recovery: `lib/sales/photo-evidence-manual-upload-client.ts` and `tests/sales-photo-evidence-manual-upload-client.test.ts` now treat the local event sync marker as a bounded optimization and preserve the authenticated BFF as the final source/authorization authority.
- Canonical event reconciliation: the manual upload client resolves a stale local sale ID only from one exact authorized Cloud match (market, `deal_closed` type, actor, and millisecond timestamp), while `lib/sync/local-cache-writer.ts`, `lib/sync/owner-pull-service.ts`, and `tests/sync-flow-audit.test.ts` mark same-ID Cloud-confirmed local events as synced instead of silently skipping their stale status.
- Database hardening draft: `supabase/migrations/057_harden_sales_photo_evidence_api_boundary.sql`.
- Server-only mutation capability: `lib/supabase/sales-photo-evidence-server-mutation-repository.server.ts` and the split read/mutation repository wiring.
- Additive server-only mutation capability: `supabase/migrations/058_add_sales_photo_evidence_server_mutation_rpcs.sql`.
- Locally verified permission cutover draft: `supabase/migrations/059_enforce_sales_photo_evidence_server_mutation_boundary.sql`; real PostgreSQL/Supabase staging execution remains required before cutover.
- Deployment support: `.env.example`, `DEPLOYMENT_CHECKLIST.md`, `scripts/build-mobile.mjs`, `scripts/smoke-mobile-static-output.mjs`, `scripts/smoke-vercel-api.mjs`, `package.json`.
- Server-only boundary support: `server-only@0.0.1`, `package-lock.json`, and the per-test React server condition in `scripts/run-tests.mjs`.
- Phase 2 verification: API URL/transport/error/auth/CORS/health tests, upload/read boundary tests, R2/payload tests, migration tests, mobile route/static-output tests, and `scripts/test-files.txt`.
- Server mutation verification: `tests/app-api-server-mutation-client.test.ts`, `tests/supabase-sales-photo-evidence-server-mutation-migration.test.ts`, `tests/sales-photo-evidence-client-mutation-boundary.test.ts`, and updated upload/repository tests.
- Decision records: `docs/IOS_MOBILE_PACKAGING_DECISION_RECORD_2026_07_16.md`, `docs/IOS_PHASE2_VERCEL_BFF_DECISION_RECORD_2026_07_17.md`.
- Progress record: `docs/IOS_CAPACITOR_PROGRESS.md`.
- Unrelated modified and untracked workspace files that predated these phases were preserved.

## Verification

- Gate 1 packaging evidence remains valid: the bundled mobile static build opens without a Next route runtime, excludes API/dynamic/Web-only handlers, and passed generic-server/browser/runtime smoke.
- Current post-split `npm.cmd test`: PASS. All 221 manifest files exited successfully in 229.8 seconds, including the 058/059 migration contracts, upload lease/attempt behavior, production mutation route path, revoked-staff cleanup, route/log secret canary, and server-only import boundary.
- `npm.cmd run build`: PASS in 123.2 seconds. The Web build retained `/api/health`, upload, and image handlers and generated 25 routes/pages.
- Web client bundle canary scan: PASS. `.next/static` contained zero occurrences of the fake server secret, `SUPABASE_SECRET_KEY`, `sb_secret_`, or any of the three privileged RPC names.
- `npm.cmd run build:mobile:api` with `https://markit-app-mocha.vercel.app`: PASS in 91.7 seconds. It generated 25 static pages and 304 files (17.98 MiB), embedded the confirmed production HTTPS API base in one artifact, and emitted zero server secret/RPC markers.
- `npm.cmd run verify:mobile`: PASS. `npm.cmd run smoke:mobile`: PASS for nine artifacts/routes, including bundled API 404 boundaries.
- `tsc --noEmit --project tsconfig.mobile.json`: PASS in 16.9 seconds.
- Phase 2 scoped ESLint: PASS with zero errors.
- Node syntax checks for the modified build/smoke scripts: PASS.
- `git diff --check`: PASS. Only line-ending normalization notices were printed by the accompanying status command.
- `http://127.0.0.1:3000`: HTTP 200 on 2026-07-17 after final builds and tests.
- Current pre-push `npm.cmd test`: PASS in 235.2 seconds after the direct-environment and local hydration decisions.
- Current clean-cache `npm.cmd run build`: PASS in 115.4 seconds; 25 static pages were generated and `/api/health`, photo upload, and photo image read remained dynamic server routes.
- Pre-push changed-file secret scan: PASS. The only token-shaped match is the deliberate fake Supabase canary in a server-secret leak regression test; `.env.example` contains placeholders only.
- Production probe on 2026-07-19: `/api/health` is HTTP 200 with `{"ok":true,"status":"healthy"}` and `Cache-Control: no-store`.
- `npm.cmd run smoke:api:staging` against `https://markit-app-mocha.vercel.app`: PASS on 2026-07-19 for health, the exact `capacitor://localhost` CORS preflight, disallowed-origin rejection, and invalid-token rejection. This smoke is read-only and does not replace real authenticated Supabase/R2 post-cutover verification.
- Controlled owner smoke passed on 2026-07-20 with explicit user authorization. Disposable market `[TEST][P2] Owner Photo Smoke 2026-07-20` (`745efd20-3b6e-4436-9b51-8432d37811f9`) is isolated, requires photo evidence, and remains available as a test fixture. One NT$1 cash sale produced exactly one pending task; `public/logo.png` was selected, processed, uploaded, and reduced the pending count from one to zero. The owner overview progressed from `照片讀取中` to an actual `最近一筆成交照片` image for that NT$1 sale, proving the deployed authenticated upload and owner read path against Supabase/R2.
- Controlled active-staff smoke passed on 2026-07-20 after the user manually signed in to the existing operator account. The employee workbench opened the retained test market, created a second NT$1 cash sale, produced one pending photo task, uploaded `public/logo.png`, and reduced the pending count from one to zero. The market totals became NT$2 across two sales. Browser diagnostics contained no error-level entries; only the existing scoped-dataset and semantic-duplicate sync warnings were emitted.
- The authorized relationship-removal step passed on 2026-07-20. The owner team page removed `1234@1234`, retained only `111@111`, displayed `已移除員工 1234@1234`, and emitted no error-level browser logs. This proves the revoke mutation completed, but revoked-user denial is not yet proven until that account signs in again and attempts controlled access/upload.
- Revoked-staff market access denial passed on 2026-07-20. After signing `1234@1234` back in and allowing the revoke cleanup to finish in a single App tab, the account resolved to its own owner identity, did not list the retained owner test market, and a direct request for market `745efd20-3b6e-4436-9b51-8432d37811f9` rendered `找不到此市集`. Cloud hydration reported the unavailable records as deleted or unauthorized. No photo upload POST was sent, so live upload-route denial remains outstanding.
- Relationship restoration preparation passed on 2026-07-20. The owner re-invited `1234@1234`; the revoked row returned to `pending`, the UI showed `待接受`, and its prior `operator`/`出攤助手` role was preserved. No error-level browser log was emitted.
- The restored relationship became active on 2026-07-20. After `1234@1234` signed in again, the account page reported `團隊身分：operator`, the retained owner market was accessible, and one controlled NT$1 cash sale created exactly one pending photo task. The task was deliberately left at `待補照片 1 筆` without selecting or uploading a file so it can be used for live revoke-during-upload evidence.
- Live revoke-during-upload denial passed on 2026-07-20 using separate authenticated owner and staff browser sessions. While `1234@1234` was active, `public/logo.png` was selected and fully prepared but not submitted. The owner then removed the relationship and the staff immediately submitted the upload. The deployed BFF returned the permission denial surfaced as `目前帳號沒有上傳這筆成交照片的權限，照片仍保留在此裝置。`; the pending count remained one, the retained preview remained available, and the action became `重新上傳`. No error-level browser log was emitted. This proves current-role authorization is enforced at POST time rather than only when the task or file picker opens.
- The owner re-invited `1234@1234` immediately after the denial and the staff accepted the invitation and switched back to the owner workspace. The restored relationship is active with the original `operator` / `現場紀錄` role.
- The post-restoration retry exposed a reproducible local/cloud reconciliation defect. The owner sees the 02:19 NT$1 sale in Cloud and totals of NT$3 across three sales, but the owner photo album contains only the two older uploaded rows and reports pending zero. The staff device retains the third local payload and reports pending one, while its retry stops at `sale_event_sync_pending` because its IndexedDB event sync marker did not recover after revoke cleanup/rejoin. The upload did not reach the BFF on this retry and no third R2/evidence row was created.
- A local recovery fix now waits for the normal local sync path but no longer treats a stale IndexedDB marker as authoritative. After the bounded wait it sends one authenticated POST to the BFF, whose existing metadata-claim path rechecks the sale event, current role, owner/market scope, and existing evidence before any R2 write. Server `source_invalid` rejection keeps the payload and uses the sync-pending user message. The focused manual-upload regression test and scoped ESLint both pass; production validation still requires deployment.
- Post-fix `npm.cmd test`: PASS on 2026-07-20. The complete manifest finished in 223.4 seconds with zero failures, including revoked-staff cleanup, server-only mutation RPCs, upload lease/attempt behavior, R2 boundaries, and the no-automatic-POST-retry contract. `tsc --noEmit --project tsconfig.mobile.json`: PASS in 13.9 seconds. `git diff --check`: PASS.
- The user pushed commit `e93b15b` (`Relax local sync gating for photo uploads`) to `main`; local `HEAD`, `origin/main`, and the public Vercel splash all confirmed `e93b15b`. A fresh staff retry on that deployed version still retained the photo and returned the sync/source-invalid message. Because `e93b15b` no longer exits before POST when the local marker is stale, this proves the BFF rejected the task's original sale event ID rather than an old client bundle blocking the request. Owner Cloud data remained NT$3 across three sales, while the album remained two uploaded rows and the staff pending count remained one.
- Source review confirmed two reconciliation gaps. Staff event hydration skipped semantic duplicate `deal_closed` rows with different IDs without preserving a canonical ID mapping, and same-ID rows already present in IndexedDB were skipped without changing a stale `local_only` status to `synced`. The second local repair uses no fuzzy date/amount matching: it accepts only one authorized Cloud row matching market, type, actor, and exact millisecond timestamp, keeps the original queue ID for payload cleanup, and leaves ambiguous or missing candidates rejected with the photo retained.
- Second-repair verification: the manual upload client test and sync flow audit both pass; scoped ESLint passes with zero errors; `tsc --noEmit --project tsconfig.mobile.json` passes; and `git diff --check` passes. A first full-manifest attempt was terminated after its child process stopped progressing at `sales-photo-evidence-writer-upload-types.test.ts`; that isolated file then passed in 3.6 seconds. A clean second `npm.cmd test` run completed in 342.7 seconds with zero failures across the complete manifest.
- `server-only@0.0.1` was installed to enforce the Next server import boundary; `package.json` and `package-lock.json` changed accordingly. No real secret was installed or written to disk.

## Baseline Failures

- Standalone repository-wide `tsc --noEmit` still reports pre-existing test-fixture type errors. Production Web/mobile builds, mobile TypeScript, and the runtime test manifest pass.
- `npm audit` reports five existing advisories (two low, three moderate; no high/critical). The production-only view reports the direct Next/PostCSS chain, and the offered `--force` remediation would install a breaking/inappropriate Next version, so no automatic dependency rewrite was performed in this Phase 2 slice. The added `server-only` package is not listed as vulnerable.
- The workspace already contained unrelated modified/untracked files. They remain preserved and were not reverted.
- The read-only Vercel API boundary smoke now passes against the deployed Phase 2 BFF.
- This workspace has no Vercel CLI/project linkage/token and no Supabase CLI/project linkage/access token. External staging cannot be created non-interactively from the current environment.
- GitHub's remote default branch still points to `master`, but Vercel's confirmed Production Branch is `main`; deployment work must target `main` and must not infer deployment behavior from the GitHub default branch.
- The Phase 2 BFF is deployed and its public health/CORS/authentication boundary smoke passes.
- Historical Supabase migrations contain duplicate version prefixes. Do not run the complete chain blindly; inspect remote migration history and use the reviewed 057 -> 058 -> staging smoke -> 059 -> post-cutover sequence.
- The user reports migrations 057, 058, and 059 as applied, but their remote migration history and post-cutover privilege state have not been independently verified from this workspace. Real Supabase/R2 tests for owner, active staff, revoked staff, and unrelated users remain outstanding.
- The application `刪除記錄` confirmation explicitly states that deleting a market only hides it from the list while retaining its data. No deployed photo-delete API or R2 object lifecycle cleanup exists, so the disposable fixture is retained for the remaining role tests rather than performing a misleading partial cleanup.
- Capacitor packages and native projects are not installed. This remains intentional until Gate 2 passes and Phase 3 decisions are approved.
- The active-staff smoke emitted `[useSync] Skipping event outside local scoped dataset` and `[useSync] Skipping semantic duplicate deal_closed event during staff sync` warnings. They did not block the transaction or photo upload and are recorded as baseline synchronization warnings rather than upload-route failures.
- Revoke cleanup can remain on a skeleton when multiple App tabs hold the same IndexedDB during the delete-and-redirect sequence. Closing the extra test tabs allowed cleanup to complete and produced the correct revoked view. This multi-tab cleanup behavior is a Web UX/reliability gap and must not be mistaken for a successful denial screen.
- A revoke/rejoin cycle can leave a local sale event sync marker stale even though the sale already exists in Cloud. Commit `e93b15b` removed that marker as a client-only hard stop, but the live retry proved the retained task can also need exact canonical sale-event ID reconciliation. The second repair is verified locally but not deployed; until it is deployed, the retained 02:19 photo remains pending and the owner album under-counts that sale's missing evidence.
- A concurrent local `next build` compiled the application successfully but its type-check step read stale `.next/dev/types` generated by the active `next dev --webpack -p 3000` process and rejected an unchanged named export in the image route. The active preview process was preserved rather than deleting its `.next` directory. The last clean-cache Web build remains passed; Vercel must perform the post-fix build from a clean checkout during deployment.
- `public/sw.js` remains a shared Web asset; native suppression still depends on the future Phase 3 bootstrap.

## Next Authorized Slice

After the full local manifest passes, deploy the canonical sale-event reconciliation repair to the existing Vercel production environment, then retry the retained 02:19 photo from the restored `1234@1234` session. The expected proof is one successful POST, deletion of the local payload, staff pending zero, owner album total three/uploaded three, and no duplicate evidence or object on a second retry/refresh. Unrelated-user denial, lease/idempotency behavior under duplicate submission, storage-failure cleanup, the multi-tab revoke-cleanup gap, and a real server-side photo/R2 cleanup path remain. Gate 2 stays closed and Phase 3 remains unauthorized until this evidence passes.
