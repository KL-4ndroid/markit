# C2.21 Read-Only Cloud Consistency Verification Result

Date: 2026-06-29
Environment: user-provided Supabase SQL Editor output
Runner: user
Supabase project: not recorded in output

SQL source:

- `supabase/verification/c2_21_cloud_consistency_read_only.sql`

Safety confirmation:

- [x] Only `SELECT` statements were reported.
- [x] No migration was reported.
- [x] No RLS/policy/function/schema change was reported.
- [x] No `INSERT`, `UPDATE`, `DELETE`, `UPSERT`, or data repair was reported.

## C2.21-A Markets Projection vs Active Deal Events

Result summary:

```text
Row count: 4
Largest revenue_diff: 9757.00
Largest deal_diff: 15
Notable market_ids:
- 7dd38e98-0181-43eb-90be-af3d1211f760: revenue_diff 9757.00, deal_diff 6, active_deal_event_count 0
- 5bfb9ff4-15b3-4b5e-831c-d96439b4d0bb: revenue_diff 6602.00, deal_diff 9, active_deal_event_count 2
- 47fd48da-2a3f-47d4-b644-06c23cb5c3bc: revenue_diff 6192.00, deal_diff 15, active_deal_event_count 0
- a2bb38ce-e320-46e1-a619-a5cc5b0213f3: revenue_diff 2111.00, deal_diff 2, active_deal_event_count 23
```

Assessment:

- [ ] Pass: zero mismatch rows.
- [x] Needs review: mismatch rows exist.
- [ ] Blocked/error:

Notes:

```text
Cloud market projections are not fully derivable from active deal_closed events for 4 markets.
This does not prove data corruption by itself. Possible causes include legacy projection writes,
older imports/manual totals, missing historical deal_closed events, or event/projection drift.
Do not repair automatically.
```

## C2.21-B Tombstone Completeness

Result summary:

```text
deal_deleted tombstone_count: 37
deal_deleted missing_root_market_id: 0
deal_deleted missing_target_event_id: 0
interaction_deleted tombstone_count: 1
interaction_deleted missing_root_market_id: 0
interaction_deleted missing_target_event_id: 0
```

Assessment:

- [x] Pass: all missing counts are zero.
- [ ] Needs review: missing root market id or target event id exists.
- [ ] Blocked/error:

Notes:

```text
Deletion tombstones have both root market_id and target event id.
This supports staff tombstone visibility and replay assumptions.
```

## C2.21-C Tombstone Target Integrity

Result summary:

```text
Row count: 0
Notable tombstone_ids: none
Notable target_event_ids: none
```

Assessment:

- [x] Pass: zero missing target rows.
- [ ] Needs review: tombstones reference missing targets.
- [ ] Blocked/error:

Notes:

```text
No tombstones reference missing target events.
```

## C2.21-D Duplicate Semantic Deal Check

Result summary:

```text
Row count: 34
Highest duplicate_count: 5
Distinct market_ids in result: 14
Highest duplicate cluster:
- market_id 886c8aab-75f6-4b74-b9a8-c11756f0a895
- deal_date 2026-02-20
- revenue 400
- duplicate_count 5
```

Assessment:

- [ ] Pass: zero duplicate semantic deal rows.
- [x] Needs manual classification: duplicate-like rows exist.
- [ ] Blocked/error:

Notes:

```text
The duplicate semantic check is intentionally broad: same market, same date, same revenue.
These rows may represent legitimate separate sales with identical totals.
They must not be auto-deleted or auto-merged.
Before any repair or replace-cache execute decision, use a follow-up read-only query to inspect
payload, timestamp, created_at, actor_id, and metadata for the duplicate clusters.
```

## C2.21-E Snapshot Shape Check

Result summary:

```text
Row count: 4
Observed data_type values: object
Observed top_level_key values: compressed
Latest snapshot_at: 2026-06-12 09:03:35.043+00
Observed versions: 1, 2, 3
Observed event_count values: 196, 24, 55, 1000
```

Assessment:

- [ ] Pass: snapshot shape is understood and compatible with current expectations.
- [x] Needs review: compressed snapshot shape cannot be inspected by simple JSON projection SQL.
- [ ] Blocked/error:

Notes:

```text
Current snapshots use a compressed top-level shape.
The old simple snapshot projection query is not sufficient for consistency verification.
Any future snapshot/rebuild audit must use the application decompression path or a dedicated
read-only decompression-aware verification tool.
```

## Overall C2.21 Decision

Overall result:

- [ ] Pass: no data-debt finding blocks future planning.
- [x] Pass with notes: findings exist but are understood and not blocking C2.20 read-only verification.
- [ ] Needs follow-up analysis before C2.20.
- [x] Requires explicit repair decision before future replace-cache/cloud-first execute work.

Recommended next step:

```text
Continue to C2.20 read-only Staff Data Flow verification.
C2.21 found projection/event drift and duplicate-like deal clusters, but tombstone structure is healthy.
These findings should block destructive repair and replace-cache execute decisions until separately classified.
They do not block the next read-only audit phase.
```
