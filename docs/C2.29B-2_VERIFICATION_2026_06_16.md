# C2.29B-2.3 E1-E5 全鏈路驗證（2026-06-16）

> **狀態**：🟡 **驗證 SQL 框架已建立，待用戶於 Supabase SQL Editor 線上執行**
> **對應任務**：C2.29B-2.3（C2.29 收斂的全鏈路驗證）
> **建立者**：Cursor (Codex)
> **建立日期**：2026-06-16
> **對應前置任務**：
> - C2.29B-1（039）✅ 已套用（2026-06-15）
> - C2.29B-1.1（040）✅ 已套用（2026-06-16）
> - C2.29B-2.1（041）✅ 已套用（2026-06-16）
>
> **前情提要**：E1-E3 在 2026-06-15 線上實測過（見 `C2.29_REANALYSIS_2026_06_15.md` §9.6）；E4 為 C2.29B-2.1 套用後的新攻擊面驗證；E5 為前端 build-time guard（待 C2.29B-2.2 實作）。

---

## 0. 為何需要 C2.29B-2.3

C2.29 收斂的最後一塊拼圖是**量化驗證 5 個攻擊面**是否真正消除：

| 攻擊面 | 對應修法 | 修法狀態 |
|---|---|---|
| E1 Staff 透過 `staff_accessible_markets` 拿 `booth_cost` | 039 改用 NULL | ✅ 已修 |
| E2 Staff 透過 `staff_accessible_products` 拿 `cost` | 039 改用 NULL | ✅ 已修 |
| E3 Staff 透過 `staff_accessible_events.payload` 拿 `boothCost` | 039 改用 NULL | ✅ 已修 |
| E4 Staff 透過底表 `markets` / `products` / `events` 拿完整資料 | **041 改 owner-only** | ✅ 已修 |
| E5 前端 build-time 阻擋員工 session 走底表 | C2.29B-2.2 type-level guard | ⏸ 待實作 |

**E1-E4 已具備線上驗證條件**（4 個 migration 已套用），**E5 需待 C2.29B-2.2 完成後才能驗證**。

---

## 1. 驗證總覽

| 驗證 | 對應攻擊面 | 攻擊者路徑 | 預期結果（4 個 migration 套用後）|
|---|---|---|---|
| **E1** | view 層欄位洩漏 | Staff DevTools → `staff_accessible_markets` | `booth_cost` / `total_profit` / `commission_rate` / 4 rental / `registration_fee` 全部 NULL |
| **E2** | view 層欄位洩漏 | Staff DevTools → `staff_accessible_products` | `cost` NULL |
| **E3** | view 層 payload 洩漏 | Staff DevTools → `staff_accessible_events` | `payload` 內 `boothCost` / `cost` / `supplierInfo` 等敏感 key 不存在 |
| **E4** | 底表 RLS 直接 SELECT | Staff DevTools → `from('markets')` / `from('products')` / `from('events')` | 全部 0 row |
| **E5** | 前端 type-level guard | Owner / Staff 編譯期試圖走底表 | build error（**待 C2.29B-2.2**）|

---

## 2. 線上驗證環境

| 項目 | 值 |
|---|---|
| 套用對象 | Supabase production |
| 套用時間 | 2026-06-15 ~ 2026-06-16 |
| 已套用 migration | 039 / 040 / 041 |
| 驗證時間 | 2026-06-16 |
| 驗證者 | 用戶（手動）|

### 2.1 測試帳號

| 角色 | UUID | 用途 |
|---|---|---|
| **Staff** | `5e92b457-1eaf-49eb-9295-ba47b5a3e575`（email: `1212@1234`）| 模擬員工 session |
| **Owner** | `0d21abfe-136f-4c42-987b-14928593f323` | 模擬老闆 session |

### 2.2 已驗證（2026-06-16 C2.29B-2.1 套用時）

E4 的 Staff direct SELECT 驗證在 C2.29B-2.1 套用時已跑過，結果如下：

```text
staff_markets_direct = 0      ✅
staff_products_direct = 0     ✅
staff_events_direct = 0         ✅
```

> 本文件 C2.29B-2.3 階段重跑 E1-E4 + 補上 E4 完整驗證 + 預備 E5 驗證 SQL。

---

## 3. 驗證 SQL（E1-E5）

### 3.1 E1: Staff 透過 `staff_accessible_markets` 拿 `booth_cost`

```sql
-- 模擬 staff session
BEGIN;
SET LOCAL ROLE authenticated;
SELECT set_config('request.jwt.claim.sub', '5e92b457-1eaf-49eb-9295-ba47b5a3e575', true);

-- E1.1 count
SELECT count(*) AS staff_markets_view FROM staff_accessible_markets;
-- 預期：> 0（員工仍可看到市集列表）

-- E1.2 敏感欄位脫敏驗證
SELECT
  count(*) FILTER (WHERE booth_cost IS NULL) AS booth_cost_null,
  count(*) FILTER (WHERE total_profit IS NULL) AS total_profit_null,
  count(*) FILTER (WHERE commission_rate IS NULL) AS commission_rate_null,
  count(*) FILTER (WHERE registration_fee IS NULL) AS registration_fee_null,
  count(*) FILTER (WHERE table_rental IS NULL) AS table_rental_null,
  count(*) FILTER (WHERE chair_rental IS NULL) AS chair_rental_null,
  count(*) FILTER (WHERE umbrella_rental IS NULL) AS umbrella_rental_null,
  count(*) FILTER (WHERE tablecloth_rental IS NULL) AS tablecloth_rental_null
FROM staff_accessible_markets;
-- 預期：8 個 FILTER 全部 = staff_markets_view 總數（全部 NULL）

ROLLBACK;
```

**驗收**：
- ✅ `staff_markets_view > 0`（員工仍可看市集列表）
- ✅ 8 個敏感欄位全部 NULL
- ✅ `deposit` / `total_revenue` / `total_interactions` / `total_deals` / `table_free` / `chair_free` 等保留

### 3.2 E2: Staff 透過 `staff_accessible_products` 拿 `cost`

```sql
-- 模擬 staff session
BEGIN;
SET LOCAL ROLE authenticated;
SELECT set_config('request.jwt.claim.sub', '5e92b457-1eaf-49eb-9295-ba47b5a3e575', true);

-- E2.1 count
SELECT count(*) AS staff_products_view FROM staff_accessible_products;
-- 預期：> 0

-- E2.2 cost 脫敏驗證
SELECT
  count(*) AS total,
  count(*) FILTER (WHERE cost IS NULL) AS cost_null
FROM staff_accessible_products;
-- 預期：total = cost_null（cost 全部 NULL）

ROLLBACK;
```

**驗收**：
- ✅ `staff_products_view > 0`
- ✅ `cost` 全部 NULL
- ✅ `price` / `stock` / `name` 等保留

### 3.3 E3: Staff 透過 `staff_accessible_events.payload` 拿 `boothCost` / `cost` / `supplierInfo`

```sql
-- 模擬 staff session
BEGIN;
SET LOCAL ROLE authenticated;
SELECT set_config('request.jwt.claim.sub', '5e92b457-1eaf-49eb-9295-ba47b5a3e575', true);

-- E3.1 count
SELECT count(*) AS staff_events_view FROM staff_accessible_events;
-- 預期：> 0

-- E3.2 敏感 key 不存在
SELECT
  count(*) FILTER (WHERE payload ? 'boothCost') AS has_boothCost,
  count(*) FILTER (WHERE payload ? 'cost') AS has_cost,
  count(*) FILTER (WHERE payload ? 'costAtTimeOfSale') AS has_costAtTimeOfSale,
  count(*) FILTER (WHERE payload ? 'supplierInfo') AS has_supplierInfo,
  count(*) FILTER (WHERE payload ? 'profitMargin') AS has_profitMargin,
  count(*) FILTER (WHERE payload ? 'grossMargin') AS has_grossMargin,
  count(*) FILTER (WHERE payload ? 'totalProfit') AS has_totalProfit,
  count(*) FILTER (WHERE payload ? 'netProfit') AS has_netProfit,
  count(*) FILTER (WHERE payload ? 'deposit') AS has_deposit,
  count(*) FILTER (WHERE payload ? 'commissionRate') AS has_commissionRate,
  count(*) FILTER (WHERE payload ? 'tableRental') AS has_tableRental,
  count(*) FILTER (WHERE payload ? 'chairRental') AS has_chairRental,
  count(*) FILTER (WHERE payload ? 'umbrellaRental') AS has_umbrellaRental,
  count(*) FILTER (WHERE payload ? 'tableclothRental') AS has_tableclothRental,
  count(*) FILTER (WHERE payload ? 'registrationFee') AS has_registrationFee,
  count(*) FILTER (WHERE payload ? 'booth_cost') AS has_booth_cost,
  count(*) FILTER (WHERE payload ? 'total_cost') AS has_total_cost
FROM staff_accessible_events;
-- 預期：所有 has_* 全部 = 0（無敏感 key）

-- E3.3 tombstone 仍可見（攻擊面修復不犧牲 tombstone）
SELECT type, count(*)
FROM staff_accessible_events
WHERE type IN ('deal_deleted', 'interaction_deleted')
GROUP BY type;
-- 預期：tombstone 仍可見

ROLLBACK;
```

**驗收**：
- ✅ 17 個敏感 key 全部 has_* = 0
- ✅ `deal_deleted` / `interaction_deleted` tombstone 仍可見
- ✅ `payload` 內其他非敏感欄位（如 `name` / `date`）保留

### 3.4 E4: Staff 透過底表 RLS 直接 SELECT（C2.29B-2.1 新攻擊面驗證）

```sql
-- 模擬 staff session
BEGIN;
SET LOCAL ROLE authenticated;
SELECT set_config('request.jwt.claim.sub', '5e92b457-1eaf-49eb-9295-ba47b5a3e575', true);

-- E4.1 markets 底表
SELECT count(*) AS staff_markets_direct FROM markets;
-- 預期：0（041 owner-only 拒絕）

-- E4.2 products 底表
SELECT count(*) AS staff_products_direct FROM products;
-- 預期：0

-- E4.3 events 底表
SELECT count(*) AS staff_events_direct FROM events;
-- 預期：0

-- E4.4 額外驗證：staff 嘗試用 SELECT 拿任何欄位都應是 0 row
SELECT count(*) AS staff_markets_with_booth_cost
FROM markets
WHERE booth_cost IS NOT NULL;
-- 預期：0

SELECT count(*) AS staff_products_with_cost
FROM products
WHERE cost IS NOT NULL;
-- 預期：0

SELECT count(*) AS staff_events_with_payload
FROM events
WHERE payload IS NOT NULL;
-- 預期：0

ROLLBACK;
```

**驗收**：
- ✅ 3 個底表 count 全部 = 0
- ✅ 即使加 WHERE IS NOT NULL 條件也 = 0
- ✅ Owner / 其他用戶不受影響（見 E4.5 + E4.6）

```sql
-- E4.5 Owner 透過底表 SELECT 完整資料（regression 驗證）
BEGIN;
SET LOCAL ROLE authenticated;
SELECT set_config('request.jwt.claim.sub', '0d21abfe-136f-4c42-987b-14928593f323', true);

SELECT count(*) AS owner_markets_direct FROM markets;
-- 預期：> 0（44 個市集）
SELECT count(*) AS owner_products_direct FROM products;
-- 預期：> 0（8 個商品）
SELECT count(*) AS owner_events_direct FROM events;
-- 預期：> 0（470 個 events）

-- E4.6 Owner 完整 financial fields
SELECT id, name, booth_cost, total_profit, commission_rate
FROM markets
LIMIT 5;
-- 預期：booth_cost / total_profit / commission_rate 為真實值

ROLLBACK;
```

### 3.5 E5: 前端 type-level guard（**待 C2.29B-2.2 實作**）

> ⚠️ **E5 無法在 2026-06-16 驗證**（C2.29B-2.2 尚未實作）。

**預計驗證方法**（C2.29B-2.2 完成後）：

```typescript
// E5.1 員工 session 試圖從底表 select（編譯期應報錯）
import { createStaffClient } from '@/lib/supabase/typed-client';

const staffClient = createStaffClient(staffUserId, isStaff=true);

// ❌ 編譯期報錯：Type 'false' does not satisfy the constraint 'true'
const { data } = await staffClient
  .from('markets')   // 員工不可走底表
  .select('*');

// ✅ 編譯期通過：員工可走 view
const { data } = await staffClient
  .from('staff_accessible_markets')  // OK
  .select('*');
```

**驗收**（C2.29B-2.2 完成後）：
- ✅ 員工 session 呼叫 `from('markets')` / `from('products')` / `from('events')` → TypeScript build error
- ✅ Owner session 同樣呼叫 → 編譯期通過
- ✅ 員工 session 呼叫 `from('staff_accessible_*')` → 編譯期通過

---

## 4. 驗證結果（2026-06-16 已跑過的 E1-E4 部分）

> 本節為 C2.29B-2.3 階段的**部分結果**。完整 5 個驗證待用戶在 Supabase SQL Editor 執行 §3 SQL 後補上。

### 4.1 E1（已驗證，C2.29B-1 套用時）

```text
booth_cost_null = 28 / 28       ✅
total_profit_null = 28 / 28     ✅
commission_rate_null = 28 / 28  ✅
table_rental_null = 28 / 28     ✅
chair_rental_null = 28 / 28     ✅
umbrella_rental_null = 28 / 28  ✅
tablecloth_rental_null = 28 / 28 ✅
registration_fee_null = 28 / 28 ✅
```

### 4.2 E2（已驗證，C2.29B-1 套用時）

```text
cost_null = 2 / 2  ✅
```

### 4.3 E3（已驗證，C2.29B-1 套用時）

```text
has_boothCost = 0         ✅
has_cost = 0              ✅
has_costAtTimeOfSale = 0  ✅
has_supplierInfo = 0      ✅
has_profitMargin = 0      ✅
has_grossMargin = 0       ✅
has_totalProfit = 0       ✅
has_netProfit = 0         ✅
has_deposit = 0           ✅
has_commissionRate = 0    ✅
has_tableRental = 0       ✅
has_chairRental = 0       ✅
has_umbrellaRental = 0    ✅
has_tableclothRental = 0  ✅
has_registrationFee = 0   ✅
has_booth_cost = 0        ✅
has_total_cost = 0        ✅
```

Tombstone 仍可見（`deal_deleted` / `interaction_deleted` 員工仍可查詢）。

### 4.4 E4（已驗證，C2.29B-2.1 套用時）

```text
staff_markets_direct = 0      ✅
staff_products_direct = 0     ✅
staff_events_direct = 0         ✅

owner_markets_direct = 44   ✅
owner_products_direct = 8    ✅
owner_events_direct = 470   ✅
```

### 4.5 E5（**待 C2.29B-2.2 實作後驗證**）

```text
[ ] 員工 session 從底表 select → build error ⏸ 待驗證
[ ] Owner session 從底表 select → 編譯期通過 ⏸ 待驗證
```

---

## 5. 待驗證項目（用戶需於 Supabase SQL Editor 執行）

> 雖然 §4 已有 4 個驗證區塊的結果，但 C2.29B-2.3 階段要求「重跑」+「完整 5 個攻擊面驗證」。請用戶在 Supabase SQL Editor 跑 §3 SQL 並把結果填入下方。

| 驗證 | 對應 SQL | 預期結果 | 實測結果（用戶填）|
|---|---|---|---|
| E1.1 count | §3.1 | `staff_markets_view > 0` | ⏸ 待用戶跑 |
| E1.2 8 個敏感欄位 | §3.1 | 全部 = count | ⏸ 待用戶跑 |
| E2.1 count | §3.2 | `staff_products_view > 0` | ⏸ 待用戶跑 |
| E2.2 cost 脫敏 | §3.2 | 全部 NULL | ⏸ 待用戶跑 |
| E3.1 count | §3.3 | `staff_events_view > 0` | ⏸ 待用戶跑 |
| E3.2 17 個敏感 key | §3.3 | 全部 has_* = 0 | ⏸ 待用戶跑 |
| E3.3 tombstone 保留 | §3.3 | `deal_deleted` / `interaction_deleted` 可見 | ⏸ 待用戶跑 |
| E4.1 markets 底表 | §3.4 | `staff_markets_direct = 0` | ⏸ 待用戶跑 |
| E4.2 products 底表 | §3.4 | `staff_products_direct = 0` | ⏸ 待用戶跑 |
| E4.3 events 底表 | §3.4 | `staff_events_direct = 0` | ⏸ 待用戶跑 |
| E4.4 IS NOT NULL | §3.4 | 全部 0 | ⏸ 待用戶跑 |
| E4.5 Owner 不 regression | §3.4 | 44 / 8 / 470 | ⏸ 待用戶跑 |
| E4.6 Owner 完整欄位 | §3.4 | 真實值 | ⏸ 待用戶跑 |
| E5.1 build error | §3.5 | TypeScript build error | ⏸ 待 C2.29B-2.2 |

---

## 6. 攻擊面消除彙整（C2.29 收斂最終判定）

| 攻擊面 | 攻擊者路徑 | 修法 | 線上驗證 | 最終判定 |
|---|---|---|---|---|
| **#1** view 層 `markets` 欄位洩漏 | `staff_accessible_markets.booth_cost` 等 8 個 | 039 NULL | ✅ E1 | ✅ 已消除 |
| **#2** view 層 `products` 欄位洩漏 | `staff_accessible_products.cost` | 039 NULL | ✅ E2 | ✅ 已消除 |
| **#3** view 層 `events` payload 洩漏 | `staff_accessible_events.payload.boothCost` 等 | 039 過濾 payload | ✅ E3 | ✅ 已消除 |
| **#4** 底表 RLS 直接 SELECT | `from('markets')` 拿 `booth_cost` | 041 owner-only | ✅ E4 | ✅ 已消除 |
| **#5** Owner branch scope leak | `staff_accessible_markets` Owner branch | 040 改用 `m.owner_id` | ✅ 040 驗證 | ✅ 已消除 |
| **#6** `is_deleted = true` 進 Staff view | `staff_accessible_markets` | 040 加 `is_deleted` 過濾 | ✅ 040 驗證 | ✅ 已消除 |
| **#7** events view Branch 4 scope leak | `staff_accessible_events` | 040 改用 `m.owner_id` | ✅ 040 驗證 | ✅ 已消除 |
| **#8** 前端 type-level guard | 未來誤寫 | C2.29B-2.2 | ⏸ E5 | ⏸ 待 C2.29B-2.2 |

→ **C2.29 收斂 100% 達標待 E5 完成**（E1-E4 已線上驗證通過；E5 待 C2.29B-2.2 實作）。

---

## 7. 範圍限制遵守確認

- ✅ C2.29B-2.3 只跑驗證 SQL + 記錄結果
- ✅ C2.29B-2.3 不改既有程式碼
- ✅ C2.29B-2.3 不改既有 migration
- ✅ C2.29B-2.3 不新增 migration
- ✅ C2.29B-2.3 不動 view 結構
- ✅ C2.29B-2.3 不動 RLS

---

## 8. Cursor 狀態

本次（C2.29B-2.3 驗證 SQL 框架 + 部分結果）：

- ✅ 已輸出 5 個驗證 block SQL（E1 / E2 / E3 / E4 / E5）
- ✅ 已記錄 4 個驗證的已實測結果（E1 / E2 / E3 / E4）
- ✅ 已標示待用戶執行項目（E1-E4 重跑 + 填入結果）
- ✅ 已標示 E5 待 C2.29B-2.2 實作
- ❌ **沒改任何程式碼**（純 docs + 驗證 SQL 框架）
- ⏸ 用戶需於 Supabase SQL Editor 跑 §3 SQL 並填入結果

---

## 9. 相關文件

- `docs/C2.29_REANALYSIS_2026_06_15.md` — C2.29 完整分析 + E1-E3 線上驗證（2026-06-15）
- `docs/C2.29B-1_APPLICATION_CHECKLIST.md` — 039 套用 step-by-step
- `docs/C2.29B_VIEW_SCOPE_AUDIT_2026_06_15.md` — 040 view scope 修補審計
- `docs/C2.29B-2_PLAN_2026_06_16.md` — C2.29B-2 完整藍圖
- `docs/C2.29B-2_1_RLS_MIGRATION_DRAFT_2026_06_16.md` — 041 套用結果（含 E4 已驗證結果）
- `docs/C2.28B_RENDER_GUARD_2026_06_16.md` — C2.28B 頁面 render guard 收尾
- `docs/OWNER_STAFF_REVENUE_HARDENING_PLAN.md` — C2.23-C2.29 主計畫
- `docs/CONVERGENCE_ARCHIVE.md` — 收斂文件總檔

---

## 10. 下一步

| 步驟 | 啟動指令 | 狀態 |
|---|---|---|
| **用戶跑 E1-E4 SQL** | "跑 E1-E4 線上驗證"（複製 §3 SQL 到 Supabase SQL Editor）| ⏸ 等用戶 |
| **填入實測結果** | "填入 E1-E4 結果"（回傳 SQL output）| ⏸ 等用戶 |
| **C2.29B-2.2 實作** | "進 C2.29B-2.2 實作"（type-level guard）| ⏸ 等用戶啟動 |
| **E5 驗證** | "跑 E5 驗證"（C2.29B-2.2 完成後才能跑）| ⏸ 等 C2.29B-2.2 |
| **C2.29 收斂 100% 達標** | E1-E5 全部通過 | ⏸ 等 E5 |
