# 階段 1: UUID 遷移 - 執行指南

> **預計時間：** 1 週  
> **難度：** ⭐⭐⭐ 中等  
> **狀態：** 🟡 準備中

---

## 📋 概述

本階段將把所有資料表的主鍵從 Auto-Increment Integer 改為 UUID，以支援離線建立與多人協作。

### 🎯 目標
- ✅ 更新 TypeScript 類型定義（ID 從 `number` 改為 `string`）
- ✅ 更新 Dexie Schema（版本 3，支援 UUID）
- ✅ 實作平滑遷移邏輯（保留舊資料，ID 映射）
- ✅ 更新所有使用 ID 的代碼
- ✅ 測試遷移完整性

---

## 📝 任務清單

### ✅ 任務 1: 更新 TypeScript 類型定義（自動生成）
- [ ] 更新 `types/db.ts`
- [ ] ID 類型從 `number?` 改為 `string?`
- [ ] 新增協作相關欄位

### ✅ 任務 2: 更新 Dexie Schema（自動生成）
- [ ] 更新 `lib/db/index.ts`
- [ ] 新增版本 3 Schema
- [ ] 實作 UUID 遷移邏輯
- [ ] 新增 UUID 生成函數

### ✅ 任務 3: 更新事件處理器（自動生成）
- [ ] 更新 `lib/db/events.ts`
- [ ] 修改所有 ID 相關邏輯

### ✅ 任務 4: 更新 React Hooks（自動生成）
- [ ] 更新 `lib/db/hooks.ts`
- [ ] 修改函數簽名（`number` → `string`）

### ✅ 任務 5: 測試遷移（手動執行）
- [ ] 備份資料（已在階段 0 完成）
- [ ] 執行遷移
- [ ] 驗證資料完整性
- [ ] 驗證關聯正確性

---

## 🚀 開始執行

我會為你生成以下檔案：

1. **`types/db.ts`** - 更新後的類型定義（UUID 版本）
2. **`lib/db/index.ts`** - 更新後的 Dexie Schema（版本 3）
3. **`lib/db/events.ts`** - 更新後的事件處理器
4. **`lib/db/hooks.ts`** - 更新後的 React Hooks
5. **`lib/db/uuid.ts`** - UUID 生成輔助函數（新增）

---

## ⚠️ 重要提醒

### 遷移前檢查清單
- [ ] ✅ 已完成階段 0（環境準備）
- [ ] ✅ 已備份 IndexedDB 資料
- [ ] ✅ 已關閉所有其他瀏覽器分頁
- [ ] ✅ 開發伺服器正在運行

### 遷移過程
1. 我會生成所有新代碼
2. 你需要重啟開發伺服器（`npm run dev`）
3. 打開應用程式，Dexie 會自動執行遷移
4. 遷移過程會在 Console 中顯示進度
5. 遷移完成後，驗證資料

### 如果遷移失敗
1. 不要慌張！
2. 使用階段 0 的備份恢復資料
3. 告訴我錯誤訊息
4. 我會協助你排查問題

---

## 📊 預期結果

遷移成功後，你會看到：

```
🔄 開始遷移到 UUID...
📊 遷移統計：
  - Markets: 5 筆 (舊ID → 新UUID)
  - Products: 23 筆 (舊ID → 新UUID)
  - Events: 156 筆 (舊ID → 新UUID)
  - DailyStats: 12 筆 (marketId 已更新)
✅ UUID 遷移完成
```

---

## 🎯 準備好了嗎？

請確認：
- [ ] 我已閱讀並理解本文件
- [ ] 我已完成階段 0
- [ ] 我已備份資料
- [ ] 我準備好開始了

**告訴我："生成 UUID 遷移代碼"**，我會立即為你生成所有檔案！🚀
